---
phase: 16-admin-subscription-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cenlar demand gt 1-17/supabase/migrations/20260317100000_admin_trial_count.sql
autonomous: true
requirements:
  - ADMN-03

must_haves:
  truths:
    - "get_admin_analytics RPC returns active_trial_count alongside mrr, pro_subscriber_count, elite_subscriber_count"
    - "active_trial_count reflects trainers with subscription_status = 'trialing' at call time"
    - "Existing callers of get_admin_analytics remain compatible (no removed fields)"
  artifacts:
    - path: "Cenlar demand gt 1-17/supabase/migrations/20260317100000_admin_trial_count.sql"
      provides: "DROP+recreate get_admin_analytics with active_trial_count in subscription_stats CTE"
      contains: "active_trial_count"
  key_links:
    - from: "subscription_stats CTE"
      to: "jsonb_build_object result"
      via: "'active_trial_count', (SELECT active_trial_count FROM subscription_stats)"
      pattern: "active_trial_count"
---

<objective>
Extend the get_admin_analytics Postgres RPC to include active_trial_count in its subscription_stats CTE output.

Purpose: The analytics tab currently displays MRR, Pro count, and Elite count but is missing active trial count. Plan 16-02 will wire all four metrics into AdminDashboard; this migration must exist first.
Output: Migration file that drops and recreates get_admin_analytics with active_trial_count added.
</objective>

<execution_context>
@/Users/xfiles/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xfiles/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-admin-subscription-visibility/16-RESEARCH.md
</context>

<interfaces>
<!-- Existing get_admin_analytics signature — must be preserved exactly. -->
<!-- Source: Cenlar demand gt 1-17/supabase/migrations/20260316200000_admin_mrr.sql -->

```sql
CREATE OR REPLACE FUNCTION public.get_admin_analytics(
  p_start  timestamptz,
  p_end    timestamptz,
  p_bucket text  -- 'day' | 'week' | 'month'
)
RETURNS jsonb
LANGUAGE plpgsql
SECURITY DEFINER
```

Current subscription_stats CTE fields (Phase 13):
- pro_subscriber_count
- elite_subscriber_count
- mrr

Current jsonb_build_object keys:
- 'totals', 'top_earners', 'mrr', 'pro_subscriber_count', 'elite_subscriber_count'

trainer_profiles.subscription_status type: 'inactive' | 'trialing' | 'active' | 'past_due' | 'canceled' | 'paused' | 'incomplete'
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migration — add active_trial_count to get_admin_analytics</name>
  <files>Cenlar demand gt 1-17/supabase/migrations/20260317100000_admin_trial_count.sql</files>
  <action>
Create the migration file at the exact path above. Copy the full get_admin_analytics function body from `20260316200000_admin_mrr.sql` — do not summarize or abbreviate. Make exactly two additions:

1. In the subscription_stats CTE, add this aggregate AFTER the mrr CASE block:
   ```sql
   COUNT(*) FILTER (WHERE subscription_status = 'trialing') AS active_trial_count
   ```

2. In the jsonb_build_object at the end, add this key after 'elite_subscriber_count':
   ```sql
   'active_trial_count', (SELECT active_trial_count FROM subscription_stats)
   ```

Preserve all other CTEs (platform_totals, top_earners, subscription_stats), the admin role guard, SECURITY DEFINER, and the GRANT statement unchanged.

Wrap in `BEGIN; ... COMMIT;` (same as Phase 13 pattern).

File header comment: `-- Phase 16: Admin Subscription Visibility — add active_trial_count to get_admin_analytics`
  </action>
  <verify>
    <automated>grep -n "active_trial_count" "Cenlar demand gt 1-17/supabase/migrations/20260317100000_admin_trial_count.sql" | wc -l | tr -d ' ' | grep -qx '3' && echo PASS || echo FAIL</automated>
  </verify>
  <done>
Migration file exists. The string "active_trial_count" appears exactly 3 times: once in the CTE COUNT(*) FILTER expression, once in the jsonb_build_object key, and once in the (SELECT active_trial_count FROM subscription_stats) subquery. All existing fields (mrr, pro_subscriber_count, elite_subscriber_count, totals, top_earners) remain in the output.
  </done>
</task>

</tasks>

<verification>
After migration file is written:
- `grep active_trial_count "Cenlar demand gt 1-17/supabase/migrations/20260317100000_admin_trial_count.sql"` returns 3 matches
- `grep "subscription_status = 'trialing'" "Cenlar demand gt 1-17/supabase/migrations/20260317100000_admin_trial_count.sql"` returns 1 match
- File still contains BEGIN and COMMIT
- File still contains `GRANT EXECUTE ON FUNCTION public.get_admin_analytics`
- All five existing RPC output keys are still present: totals, top_earners, mrr, pro_subscriber_count, elite_subscriber_count
</verification>

<success_criteria>
Migration exists at the correct path. Running it against the Supabase DB will produce a get_admin_analytics that returns a sixth key, active_trial_count, reflecting the current count of trialing trainer subscriptions. No existing callers break.
</success_criteria>

<output>
After completion, create `.planning/phases/16-admin-subscription-visibility/16-01-SUMMARY.md`
</output>
