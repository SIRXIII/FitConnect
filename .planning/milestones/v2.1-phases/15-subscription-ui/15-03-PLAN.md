---
phase: 15-subscription-ui
plan: 03
type: execute
wave: 3
depends_on: [15-01, 15-02]
files_modified:
  - Cenlar demand gt 1-17/src/components/subscription/TrialBanner.tsx
  - Cenlar demand gt 1-17/src/components/subscription/SubscriptionTab.tsx
  - Cenlar demand gt 1-17/src/components/subscription/DowngradeModal.tsx
  - Cenlar demand gt 1-17/src/pages/TrainerDashboard.tsx
  - Cenlar demand gt 1-17/src/App.tsx
autonomous: true
requirements: [BILL-02, BILL-03, BILL-05]

must_haves:
  truths:
    - "Trainer trialing with 6 days left sees a persistent banner on all pages: 'X days left in your Pro trial -- add payment to keep access'"
    - "Banner is absent when trial has more than 7 days remaining"
    - "TrainerDashboard has a 4th tab 'subscription' showing current plan, tier badge, and 'Manage Subscription' button"
    - "Tier badge shows 'Pro -- Trialing' with trial end date when trialing"
    - "Clicking 'Manage Subscription' calls getPortalUrl() and redirects to Stripe Customer Portal"
    - "Clicking 'Downgrade to Free' shows a confirmation modal listing exact features that will be lost"
    - "DowngradeModal dynamically computes lost features from TIER_GATES"
  artifacts:
    - path: "Cenlar demand gt 1-17/src/components/subscription/TrialBanner.tsx"
      provides: "Persistent trial countdown banner"
      min_lines: 20
    - path: "Cenlar demand gt 1-17/src/components/subscription/SubscriptionTab.tsx"
      provides: "Dashboard subscription management tab"
      min_lines: 50
    - path: "Cenlar demand gt 1-17/src/components/subscription/DowngradeModal.tsx"
      provides: "Confirmation modal listing features lost on downgrade"
      min_lines: 40
  key_links:
    - from: "Cenlar demand gt 1-17/src/App.tsx"
      to: "src/components/subscription/TrialBanner.tsx"
      via: "Rendered between Navbar and Routes for all-page persistence"
      pattern: "TrialBanner"
    - from: "Cenlar demand gt 1-17/src/pages/TrainerDashboard.tsx"
      to: "src/components/subscription/SubscriptionTab.tsx"
      via: "activeTab === 'subscription' renders SubscriptionTab"
      pattern: "subscription.*SubscriptionTab"
    - from: "Cenlar demand gt 1-17/src/components/subscription/SubscriptionTab.tsx"
      to: "src/lib/subscription.ts"
      via: "getPortalUrl() for Stripe Portal redirect"
      pattern: "getPortalUrl"
    - from: "Cenlar demand gt 1-17/src/components/subscription/SubscriptionTab.tsx"
      to: "src/components/subscription/DowngradeModal.tsx"
      via: "showDowngradeModal state toggles modal visibility"
      pattern: "DowngradeModal"
    - from: "Cenlar demand gt 1-17/src/components/subscription/DowngradeModal.tsx"
      to: "src/lib/subscription.ts"
      via: "featuresLostOnDowngrade(currentTier, 'free')"
      pattern: "featuresLostOnDowngrade"
---

<objective>
Build the trial banner, subscription management tab, and downgrade confirmation modal.

Purpose: Trainers see trial countdown (success criteria 3), manage their subscription from the dashboard (success criteria 4), and get a confirmation modal before downgrading (success criteria 5).
Output: TrialBanner in App.tsx layout, SubscriptionTab in TrainerDashboard, DowngradeModal with dynamic feature diff.
</objective>

<execution_context>
@/Users/xfiles/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xfiles/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-subscription-ui/15-RESEARCH.md
@.planning/phases/15-subscription-ui/15-01-SUMMARY.md

@Cenlar demand gt 1-17/src/App.tsx
@Cenlar demand gt 1-17/src/pages/TrainerDashboard.tsx
@Cenlar demand gt 1-17/src/stores/auth.ts
@Cenlar demand gt 1-17/src/hooks/useTier.ts
@Cenlar demand gt 1-17/src/lib/tierGates.ts

<interfaces>
<!-- From plan 15-01 outputs -->

From src/lib/subscription.ts (created in 15-01):
```typescript
export async function getPortalUrl(): Promise<{ url: string }>;
export function featuresLostOnDowngrade(from: Tier, to: Tier): TierFeature[];
```

From src/hooks/useTier.ts:
```typescript
export function useTier(): { tier: Tier; isTrialing: boolean; trialEndsAt: string | null };
export function useCan(feature: TierFeature): boolean;
```

From src/stores/auth.ts:
```typescript
// trainerProfile fields used:
//   subscription_tier: string
//   subscription_status: string
//   trial_ends_at: string | null
//   current_period_end: string | null
//   cancel_at_period_end: boolean
//   subscription_interval: string | null
```

From src/lib/tierGates.ts:
```typescript
export type TierFeature = 'slots_ten' | 'slots_unlimited' | 'extended_bio' | 'analytics_advanced' | 'priority_search' | 'featured_landing';
```

TrainerDashboard.tsx current tab structure (line 24, 179):
```typescript
const [activeTab, setActiveTab] = useState<'overview' | 'payouts' | 'analytics'>('overview');
// Tab bar: (['overview', 'payouts', 'analytics'] as const).map(tab => ...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TrialBanner and add to App.tsx layout</name>
  <files>
    Cenlar demand gt 1-17/src/components/subscription/TrialBanner.tsx
    Cenlar demand gt 1-17/src/App.tsx
  </files>
  <action>
**TrialBanner.tsx**:
- Import `useAuthStore` from `@/stores/auth`
- Reads `trainerProfile` from store
- Returns null if:
  - `!trainerProfile` (not loaded yet -- prevents flash, per pitfall 3 in research)
  - `trainerProfile.subscription_status !== 'trialing'`
  - `!trainerProfile.trial_ends_at`
  - daysLeft > 7 (success criteria: "absent when more than 7 days remain")
- Compute `daysLeft = Math.ceil((new Date(trainerProfile.trial_ends_at).getTime() - Date.now()) / (1000 * 60 * 60 * 24))`
- Derive `tierName` from subscription_tier ('Pro' or 'Elite')
- Render a banner with editorial styling:
  - Container: `bg-accent/5 border-b border-accent/10 px-6 py-3 text-center`
  - Text: `text-xs tracking-wide text-ink/70`
  - Content: "{daysLeft} day(s) left in your {tierName} trial -- " with link "add payment to keep access" pointing to `/trainer/dashboard?tab=subscription`
  - Link styled: `text-accent underline underline-offset-2`
  - Handle singular "1 day" vs plural "X days"

**App.tsx** modifications:
- Import `TrialBanner` from `@/components/subscription/TrialBanner`
- Import `useAuthStore` at top (already imported)
- Inside the App component, extract `profile` from `useAuthStore((s) => s.profile)` -- add a second selector line
- After `<Navbar />` and before `<Routes>`, add:
  ```tsx
  {profile?.role === 'trainer' && <TrialBanner />}
  ```
- This ensures the banner is persistent across ALL pages (not just dashboard), and only renders for trainers

Do NOT use the `loading` selector from auth store for the banner -- TrialBanner handles its own null checks internally.
  </action>
  <verify>
    <automated>cd "Cenlar demand gt 1-17" && grep "TrialBanner" src/App.tsx && grep "daysLeft > 7" src/components/subscription/TrialBanner.tsx && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>TrialBanner renders between Navbar and Routes for trainers; shows countdown when trial <= 7 days; returns null when > 7 days or not trialing</done>
</task>

<task type="auto">
  <name>Task 2: Create SubscriptionTab with DowngradeModal and wire into TrainerDashboard</name>
  <files>
    Cenlar demand gt 1-17/src/components/subscription/DowngradeModal.tsx
    Cenlar demand gt 1-17/src/components/subscription/SubscriptionTab.tsx
    Cenlar demand gt 1-17/src/pages/TrainerDashboard.tsx
  </files>
  <action>
**DowngradeModal.tsx**:
- Props: `{ isOpen: boolean; onClose: () => void; onConfirm: () => void; currentTier: Tier; loading: boolean }`
- If `!isOpen` return null
- Import `featuresLostOnDowngrade` from `@/lib/subscription` and `TierFeature` from `@/lib/tierGates`
- Compute `lostFeatures = featuresLostOnDowngrade(currentTier, 'free')`
- Human-readable feature names map (inline constant):
  ```
  slots_ten -> "Up to 10 visibility slots"
  slots_unlimited -> "Unlimited visibility slots"
  extended_bio -> "Extended bio (1,000 characters)"
  analytics_advanced -> "Advanced analytics dashboard"
  priority_search -> "Priority search ranking"
  featured_landing -> "Featured on landing page"
  ```
- Render a modal overlay: `fixed inset-0 z-50 flex items-center justify-center bg-ink/40`
- Modal card: `bg-paper border border-ink/10 p-8 max-w-md w-full mx-4 space-y-6`
- Title: `text-xl serif font-light italic text-ink` "Downgrade to Free?"
- Warning text: `text-sm text-ink/60` "You will lose access to these features:"
- Feature list: each lost feature with a Lucide `X` icon in red/accent, `text-sm text-ink/70`
- Two buttons row:
  - Cancel: `border border-ink/20 px-8 py-3 text-[11px] uppercase tracking-[0.2em] font-medium hover:bg-ink hover:text-white transition-all` calls onClose
  - Confirm downgrade: `bg-red-600 text-white px-8 py-3 text-[11px] uppercase tracking-[0.2em] font-medium hover:bg-red-700 transition-colors` calls onConfirm, disabled when loading

**SubscriptionTab.tsx**:
- Import `useAuthStore`, `useTier` from hooks, `getPortalUrl` from `@/lib/subscription`, `toast` from sonner, `Link` from react-router-dom
- Import `DowngradeModal`
- State: `loading: boolean`, `showDowngradeModal: boolean`
- Read `trainerProfile`, `user`, `fetchProfile` from auth store
- Read `tier`, `isTrialing`, `trialEndsAt` from useTier()
- **Tier Badge section**:
  - Display tier name (Free / Pro / Elite) with status
  - If trialing: show "{TierName} -- Trialing" with "until {formatted date}" in smaller text
  - If active (subscription_status === 'active'): show "{TierName}" with optional "Renews {current_period_end date}" or "Cancels {current_period_end date}" if cancel_at_period_end
  - If free/no subscription: show "Free" with "Start a trial to unlock more features" and Link to `/pricing`
  - Badge styling: `border border-ink/10 p-8 space-y-3`, tier name as `text-2xl serif font-light italic`
  - Status label: `text-[10px] uppercase tracking-[0.2em] text-ink/40 font-medium`

- **Actions section** (only if tier !== 'free'):
  - "Manage Subscription" button: calls `handleManageSubscription` async:
    1. setLoading(true)
    2. try: const { url } = await getPortalUrl()
    3. window.location.href = url
    4. catch: toast.error(err.message or 'Failed to open subscription portal')
    5. finally: setLoading(false)
  - Button style: `border border-accent text-accent px-8 py-3 text-[11px] uppercase tracking-[0.2em] font-medium hover:bg-accent hover:text-white transition-all duration-300`
  - "Downgrade to Free" button (only if tier is pro or elite and NOT already canceling):
    - Opens DowngradeModal: `setShowDowngradeModal(true)`
    - Button style: `text-ink/40 text-[11px] uppercase tracking-[0.2em] hover:text-red-600 transition-colors`

- **DowngradeModal** rendered at bottom:
  - onConfirm calls handleManageSubscription (redirect to Stripe Portal where actual downgrade/cancel happens)
  - Pass currentTier as `tier as Tier` (cast needed since useTier returns Tier)

- If tier is 'free', show a CTA section with Link to `/pricing`: "Explore plans" button styled as primary CTA

**TrainerDashboard.tsx** modifications:
- Import `SubscriptionTab` from `@/components/subscription/SubscriptionTab`
- Change tab type from `'overview' | 'payouts' | 'analytics'` to `'overview' | 'payouts' | 'analytics' | 'subscription'`
- Update the tab array: `(['overview', 'payouts', 'analytics', 'subscription'] as const)`
- Update `useState` initialization to read `tab` search param and include 'subscription' in validation:
  ```typescript
  const [activeTab, setActiveTab] = useState<typeof tabs[number]>(() => {
    const tabParam = searchParams.get('tab');
    return tabs.includes(tabParam as typeof tabs[number]) ? (tabParam as typeof tabs[number]) : 'overview';
  });
  ```
  (where `const tabs = ['overview', 'payouts', 'analytics', 'subscription'] as const;`)
- Add tab content rendering after the analytics tab block:
  ```tsx
  {activeTab === 'subscription' && <SubscriptionTab />}
  ```

IMPORTANT: The current tab bar uses `useSearchParams` (line 22) but does NOT read `tab` from it for initial state. The init state is hardcoded to `'overview'`. Change to read `searchParams.get('tab')` so that returning from Stripe Portal (`?tab=subscription`) auto-selects the subscription tab.
  </action>
  <verify>
    <automated>cd "Cenlar demand gt 1-17" && grep "'subscription'" src/pages/TrainerDashboard.tsx && grep "getPortalUrl" src/components/subscription/SubscriptionTab.tsx && grep "featuresLostOnDowngrade" src/components/subscription/DowngradeModal.tsx && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>TrainerDashboard has 4 tabs including 'subscription'; SubscriptionTab shows tier badge, manage button, and downgrade option; DowngradeModal lists dynamically computed features lost; ?tab=subscription query param auto-selects tab on return from Stripe Portal</done>
</task>

</tasks>

<verification>
- TrialBanner renders on all pages for trialing trainers with <= 7 days left
- TrialBanner is absent for trainers with > 7 days or non-trialing status
- TrainerDashboard shows 4 tabs: overview, payouts, analytics, subscription
- Navigating to `/trainer/dashboard?tab=subscription` auto-selects the subscription tab
- "Manage Subscription" button calls getPortalUrl and redirects
- "Downgrade to Free" opens modal listing lost features dynamically
- DowngradeModal confirm redirects to Stripe Portal
- `npx vitest run` passes (no regressions)
</verification>

<success_criteria>
- Persistent trial banner visible on all pages when trial <= 7 days remaining
- Banner absent when trial > 7 days remaining
- Subscription tab shows tier badge with "Pro -- Trialing until [date]" format
- "Manage Subscription" redirects to Stripe Customer Portal
- Downgrade modal lists correct features lost (derived from TIER_GATES)
- ?tab=subscription query param works for Stripe Portal return flow
</success_criteria>

<output>
After completion, create `.planning/phases/15-subscription-ui/15-03-SUMMARY.md`
</output>
