---
phase: 10-earnings-analytics
plan: "04"
type: execute
wave: 3
depends_on: ["10-02", "10-03"]
files_modified: []
autonomous: false
requirements: [ANALYTICS-01, ANALYTICS-02, ANALYTICS-03, ANALYTICS-04, ANALYTICS-05, ANALYTICS-06]

must_haves:
  truths:
    - "Trainer analytics tab is fully functional end-to-end with live Supabase data"
    - "Admin analytics tab shows correct platform data with top earners"
    - "CSV export downloads a correctly formatted file"
    - "All builds pass with zero TypeScript errors"
  artifacts:
    - path: "Cenlar demand gt 1-17/supabase/migrations/20260315000000_analytics_rpc.sql"
      provides: "Deployed RPC functions"
    - path: "Cenlar demand gt 1-17/src/components/trainer/AnalyticsTab.tsx"
      provides: "Trainer analytics UI"
    - path: "Cenlar demand gt 1-17/src/pages/AdminDashboard.tsx"
      provides: "Admin analytics with top earners"
  key_links:
    - from: "AnalyticsTab.tsx"
      to: "get_trainer_analytics RPC"
      via: "supabase.rpc() in useEffect"
      pattern: "supabase.rpc.*get_trainer_analytics"
    - from: "AdminDashboard.tsx"
      to: "get_admin_analytics RPC"
      via: "supabase.rpc() in useEffect"
      pattern: "supabase.rpc.*get_admin_analytics"
---

<objective>
Verify the complete Phase 10 analytics implementation end-to-end: trainer dashboard analytics, admin platform analytics, and CSV export all working correctly in the running app.

Purpose: Human eyes catch rendering issues, data accuracy, and UX problems that build checks cannot.
Output: Phase 10 marked complete after human verification.
</objective>

<execution_context>
@/Users/xfiles/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xfiles/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-earnings-analytics/10-02-SUMMARY.md
@.planning/phases/10-earnings-analytics/10-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final build verification</name>
  <files></files>
  <action>
Run full TypeScript build to confirm all Phase 10 files compile cleanly before human verification:

```bash
cd "Cenlar demand gt 1-17" && npm run build
```

Confirm:
- Exit code 0
- Zero TypeScript errors
- All new/modified files appear in the build output without warnings:
  - src/lib/analytics.ts
  - src/components/trainer/AnalyticsTab.tsx
  - src/pages/AdminDashboard.tsx
  - src/pages/TrainerDashboard.tsx

If build fails, fix TypeScript errors before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd "Cenlar demand gt 1-17" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>npm run build exits 0 with zero TypeScript errors across all Phase 10 files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — trainer and admin analytics end-to-end</name>
  <files></files>
  <action>
Human verification of Phase 10 Earnings Analytics. Run supabase db push to deploy the migration, start the dev server, then check trainer and admin analytics.

What was built:
- Trainer Analytics tab: range selector, 5 metric cards, revenue trend (AreaChart), booking count (BarChart), peak hours heatmap (CSS grid), Export CSV button
- Admin analytics tab: time period filter, 4 platform metric cards (Total Revenue, Platform Fee, Trainer Payouts, Booking Volume), Top Earners table
- Supabase migration: 3 RPC functions (get_trainer_analytics, get_trainer_peak_hours, get_admin_analytics)

Step 1 — Deploy migration:
  cd "Cenlar demand gt 1-17" && supabase db push
  Confirm 20260315000000_analytics_rpc.sql applied without error.

Step 2 — Start dev server:
  cd "Cenlar demand gt 1-17" && npm run dev

Step 3 — Trainer Analytics tab:
  1. Log in as a trainer
  2. Open Trainer Dashboard — confirm three tabs: Overview, Payouts, Analytics
  3. Click Analytics tab
  4. Verify: range selector (week/month/quarter/year), month selected by default
  5. Verify: five metric cards render (Gross Earnings, Net Earnings, Bookings, Avg Price, Discount Adoption)
  6. Verify: AreaChart and BarChart render without errors
  7. Verify: Peak hours heatmap CSS grid (7 rows x 24 columns)
  8. Click Export CSV — confirm .csv file downloads
  9. Switch range to "year" — data refetches

Step 4 — Admin analytics tab:
  1. Log in as admin
  2. Admin Dashboard → Analytics tab
  3. Verify: range selector appears
  4. Verify: four metric cards (Total Revenue, Platform Fee Collected, Trainer Payouts, Booking Volume)
  5. Verify: Top Earners table with header row
  6. Switch range — metrics update

Step 5 — No regressions:
  1. Trainer Overview tab: stats, rates, discount slider, availability still work
  2. Trainer Payouts tab: still works
  3. Admin Users, Reviews, Settings tabs: still work
  </action>
  <verify>
    <automated>cd "Cenlar demand gt 1-17" && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>Human approves all verification steps. Phase 10 complete — all six ANALYTICS requirements verified working in the running app.</done>
</task>

</tasks>

<verification>
- Build exits 0
- Trainer sees Analytics tab with all five sections
- Admin sees updated analytics tab with range selector and top earners
- CSV export works
- No regressions to existing tabs
</verification>

<success_criteria>
- Human approves all verification steps
- Phase 10 is complete: all six ANALYTICS requirements addressed
- No TypeScript errors, no runtime errors observed during verification
</success_criteria>

<output>
After completion, create `.planning/phases/10-earnings-analytics/10-04-SUMMARY.md`
</output>
