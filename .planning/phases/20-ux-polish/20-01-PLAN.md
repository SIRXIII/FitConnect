---
phase: 20-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/shared/Skeleton.tsx
  - src/components/shared/Skeleton.test.tsx
  - src/components/shared/ErrorState.tsx
  - src/components/shared/ErrorState.test.tsx
  - src/lib/errorMessages.ts
  - src/lib/imageUtils.ts
  - src/lib/imageUtils.test.ts
autonomous: true
requirements: [UXP-02, UXP-03, UXP-04]

must_haves:
  truths:
    - "Skeleton primitives render with animate-pulse and match expected dimensions"
    - "ErrorState renders title, message, retry button, and back link"
    - "mapError converts Supabase/network errors to user-friendly messages"
    - "optimizedUrl appends width/quality/format params to Unsplash URLs"
  artifacts:
    - path: "src/components/shared/Skeleton.tsx"
      provides: "SkeletonLine, SkeletonCircle, SkeletonRect primitives"
      exports: ["SkeletonLine", "SkeletonCircle", "SkeletonRect"]
    - path: "src/components/shared/ErrorState.tsx"
      provides: "Reusable error display with retry and back navigation"
      exports: ["ErrorState"]
    - path: "src/lib/errorMessages.ts"
      provides: "mapError utility for user-friendly error messages"
      exports: ["mapError"]
    - path: "src/lib/imageUtils.ts"
      provides: "optimizedUrl utility for Unsplash URL optimization"
      exports: ["optimizedUrl"]
  key_links:
    - from: "src/components/shared/ErrorState.tsx"
      to: "src/lib/errorMessages.ts"
      via: "imports mapError for default message derivation"
      pattern: "import.*mapError.*from.*errorMessages"
---

<objective>
Create the shared UX foundation components: skeleton loading primitives, reusable error state component, error message mapping utility, and image URL optimization utility.

Purpose: These are the building blocks that Plan 02 (cross-app integration) and Plan 03 (booking wizard) depend on. Creating them first with tests ensures consistent quality across the app.
Output: 4 new source files + 3 test files, all tested and passing.
</objective>

<execution_context>
@/Users/xfiles/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xfiles/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-ux-polish/20-RESEARCH.md
@.planning/phases/20-ux-polish/20-VALIDATION.md

@src/components/shared/ErrorBoundary.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create skeleton primitives and ErrorState component with tests</name>
  <files>src/components/shared/Skeleton.tsx, src/components/shared/Skeleton.test.tsx, src/components/shared/ErrorState.tsx, src/components/shared/ErrorState.test.tsx</files>
  <behavior>
    - Skeleton.test: SkeletonLine renders div with animate-pulse class and default w-full
    - Skeleton.test: SkeletonLine accepts custom width class (e.g., "w-36")
    - Skeleton.test: SkeletonCircle renders div with rounded-full and animate-pulse
    - Skeleton.test: SkeletonRect renders div with animate-pulse and accepts className
    - ErrorState.test: renders title and message text
    - ErrorState.test: renders retry button when onRetry provided, clicking it calls the handler
    - ErrorState.test: renders back link when backTo provided with correct path and label
    - ErrorState.test: does NOT render retry button when onRetry is omitted
    - ErrorState.test: uses default title "Something went wrong" when title not provided
  </behavior>
  <action>
    Create Skeleton.tsx with three named exports: SkeletonLine (accepts width?: string defaulting to "w-full" and className?: string, renders div with `h-4 ${width} bg-ink/5 animate-pulse ${className}`), SkeletonCircle (accepts size?: string defaulting to "w-10 h-10", renders div with `${size} rounded-full bg-ink/5 animate-pulse`), SkeletonRect (accepts className?: string defaulting to "h-48 w-full", renders div with `bg-ink/5 animate-pulse ${className}`). All use rounded-none (no border radius) to match the app's sharp-edge design language.

    Create ErrorState.tsx as a named export. Props interface: title?: string (default "Something went wrong"), message: string, onRetry?: () => void, backTo?: { label: string; path: string }, icon?: React.ReactNode. Renders a centered flex column with: icon container (w-14 h-14 rounded-full bg-red-50 with AlertTriangle from lucide-react as default), title in serif font-light italic, message in text-sm text-ink/50, and a flex row of optional buttons. Retry button uses RefreshCw icon + "Try Again" text. Back link uses ArrowLeft icon + backTo.label, rendered as react-router-dom Link. Buttons use the app's standard button style: border border-ink/20 px-6 py-3 text-[11px] uppercase tracking-[0.2em] font-medium hover:bg-ink hover:text-white transition-all duration-300.

    Follow the exact code patterns from 20-RESEARCH.md Architecture Patterns sections 2 and 3.
  </action>
  <verify>
    <automated>cd "Cenlar demand gt 1-17" && npx vitest run src/components/shared/Skeleton.test.tsx src/components/shared/ErrorState.test.tsx --reporter=verbose</automated>
  </verify>
  <done>Skeleton primitives and ErrorState component render correctly with all prop variations. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create error message mapping and image optimization utilities with tests</name>
  <files>src/lib/errorMessages.ts, src/lib/imageUtils.ts, src/lib/imageUtils.test.ts</files>
  <behavior>
    - imageUtils.test: optimizedUrl adds w= and q=80 and auto=format to Unsplash URLs
    - imageUtils.test: optimizedUrl preserves non-Unsplash URLs unchanged
    - imageUtils.test: optimizedUrl handles Unsplash URLs that already have query params (uses &amp; not ?)
    - imageUtils.test: optimizedUrl uses custom width when provided
    - imageUtils.test: optimizedUrl defaults to w=800
  </behavior>
  <action>
    Create src/lib/errorMessages.ts with a named export `mapError(error: unknown): AppError` where AppError = { title: string; message: string; recoverable: boolean }. Map these error patterns:
    - error.message includes "JWT" -> title: "Session expired", message: "Your session has expired. Please sign in again to continue.", recoverable: false
    - error.message includes "row-level security" -> title: "Access denied", message: "You do not have permission to perform this action.", recoverable: false
    - error.message includes "duplicate key" -> title: "Already exists", message: "This item already exists. Please check and try again.", recoverable: false
    - error.message includes "Failed to fetch" OR "NetworkError" -> title: "Connection problem", message: "Could not reach the server. Check your internet connection and try again.", recoverable: true
    - Default fallback: title: "Something went wrong", message: "An unexpected error occurred. Please try again.", recoverable: true
    Export the AppError interface as well.

    Create src/lib/imageUtils.ts with a named export `optimizedUrl(url: string, width?: number): string`. If url includes "unsplash.com", append `w=${width || 800}&q=80&auto=format` using `&` if URL already has `?`, otherwise `?`. Return non-Unsplash URLs unchanged. Also export a constant `IMAGE_ATTRS = { loading: 'lazy' as const, decoding: 'async' as const }` for convenient spread on img tags.

    Create tests for imageUtils only (errorMessages is a simple mapping utility -- coverage comes from ErrorState integration tests in Plan 02). Follow the exact patterns from 20-RESEARCH.md Code Examples section.
  </action>
  <verify>
    <automated>cd "Cenlar demand gt 1-17" && npx vitest run src/lib/imageUtils.test.ts --reporter=verbose</automated>
  </verify>
  <done>mapError handles JWT, RLS, duplicate, network, and default errors. optimizedUrl correctly optimizes Unsplash URLs and passes through others. All tests pass.</done>
</task>

</tasks>

<verification>
cd "Cenlar demand gt 1-17" && npx vitest run src/components/shared/Skeleton.test.tsx src/components/shared/ErrorState.test.tsx src/lib/imageUtils.test.ts --reporter=verbose
</verification>

<success_criteria>
- Skeleton primitives (SkeletonLine, SkeletonCircle, SkeletonRect) exported and tested
- ErrorState component exported with retry and back navigation support, tested
- mapError utility exported with 5 error pattern mappings
- optimizedUrl utility exported and tested for Unsplash URL optimization
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/20-ux-polish/20-01-SUMMARY.md`
</output>
