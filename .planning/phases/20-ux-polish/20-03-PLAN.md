---
phase: 20-ux-polish
plan: 03
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - src/components/booking/BookingWizard.tsx
  - src/components/booking/BookingWizard.test.tsx
  - src/components/booking/StepReview.tsx
  - src/components/booking/StepConfirm.tsx
  - src/components/booking/StepPayment.tsx
  - src/components/booking/StepSuccess.tsx
  - src/pages/BookSession.tsx
autonomous: true
requirements: [UXP-01]

must_haves:
  truths:
    - "Booking flow shows a numbered progress indicator with step labels"
    - "Steps transition with a smooth slide animation (Framer Motion)"
    - "Progress indicator highlights completed and current steps"
    - "User can navigate back through steps using a Back button"
    - "Payment step is conditionally included based on STRIPE_CONFIGURED"
    - "Pricing breakdown is clearly visible on the confirm step"
  artifacts:
    - path: "src/components/booking/BookingWizard.tsx"
      provides: "Step container with ProgressIndicator and AnimatePresence transitions"
      exports: ["BookingWizard"]
    - path: "src/components/booking/StepReview.tsx"
      provides: "Step 1: session details review with trainer info and notes input"
      exports: ["StepReview"]
    - path: "src/components/booking/StepConfirm.tsx"
      provides: "Step 2: pricing breakdown and confirm button"
      exports: ["StepConfirm"]
    - path: "src/components/booking/StepPayment.tsx"
      provides: "Step 3: Stripe Elements payment form wrapper"
      exports: ["StepPayment"]
    - path: "src/components/booking/StepSuccess.tsx"
      provides: "Step 4: booking confirmation with next actions"
      exports: ["StepSuccess"]
    - path: "src/pages/BookSession.tsx"
      provides: "Refactored page using BookingWizard instead of inline step rendering"
      min_lines: 150
  key_links:
    - from: "src/pages/BookSession.tsx"
      to: "src/components/booking/BookingWizard.tsx"
      via: "renders BookingWizard with slot data and callbacks"
      pattern: "BookingWizard"
    - from: "src/components/booking/BookingWizard.tsx"
      to: "framer-motion"
      via: "AnimatePresence + motion.div for step transitions"
      pattern: "AnimatePresence"
---

<objective>
Redesign the booking flow from a 628-line monolithic page into a multi-step wizard with a visual progress indicator, Framer Motion animated transitions, and clear step separation.

Purpose: Transform the booking experience to feel premium and guided. The current flow has no visual progress indication and abrupt step changes. The redesign breaks it into focused step components with smooth animations.
Output: BookingWizard container + 4 step components + refactored BookSession.tsx.
</objective>

<execution_context>
@/Users/xfiles/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xfiles/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-ux-polish/20-RESEARCH.md
@.planning/phases/20-ux-polish/20-01-SUMMARY.md

@src/pages/BookSession.tsx
@src/lib/stripe.ts

<interfaces>
<!-- From Plan 01 outputs -->
From src/components/shared/Skeleton.tsx:
```typescript
export const SkeletonLine: React.FC<{ width?: string; className?: string }>;
export const SkeletonRect: React.FC<{ className?: string }>;
```

From src/components/shared/ErrorState.tsx:
```typescript
export const ErrorState: React.FC<{
  title?: string;
  message: string;
  onRetry?: () => void;
  backTo?: { label: string; path: string };
  icon?: React.ReactNode;
}>;
```

<!-- Existing booking types from BookSession.tsx -->
From src/pages/BookSession.tsx:
```typescript
interface SlotWithTrainer extends AvailabilitySlot {
  trainer_profiles: TrainerProfile & {
    profiles: { full_name: string; avatar_url: string | null; };
  };
}
type BookingStep = 'review' | 'confirm' | 'payment' | 'success';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create BookingWizard container with progress indicator and step components</name>
  <files>
    src/components/booking/BookingWizard.tsx,
    src/components/booking/BookingWizard.test.tsx,
    src/components/booking/StepReview.tsx,
    src/components/booking/StepConfirm.tsx,
    src/components/booking/StepPayment.tsx,
    src/components/booking/StepSuccess.tsx
  </files>
  <behavior>
    - BookingWizard.test: renders ProgressIndicator with step labels
    - BookingWizard.test: first step (Review) is highlighted as current
    - BookingWizard.test: step labels show "Review", "Confirm", "Payment", "Complete" when Stripe configured
    - BookingWizard.test: step labels show "Review", "Confirm", "Complete" when Stripe NOT configured
    - BookingWizard.test: renders step content inside a motion.div container
  </behavior>
  <action>
    Create src/components/booking/ directory with these files:

    BookingWizard.tsx:
    - Props: { slot: SlotWithTrainer; onComplete: () => void; stripeConfigured: boolean; }
    - Dynamically compute STEPS array: ['Review', 'Confirm', ...(stripeConfigured ? ['Payment'] : []), 'Complete']
    - State: currentStepIndex (number), bookingId (string | null), clientSecret (string | null), notes (string)
    - ProgressIndicator component (inline or separate): renders numbered circles connected by lines. Current step: bg-accent text-white. Completed steps: bg-accent text-white. Future steps: border border-ink/20 text-ink/30. Step labels below each circle in text-[9px] uppercase tracking-[0.2em]. Use flex items-center justify-between for layout. Connecting lines: h-px w-12 md:w-24 mx-2, colored bg-accent if completed, bg-ink/10 if not.
    - AnimatePresence mode="wait" wrapping a motion.div with key={currentStepIndex}. Variants: enter (opacity: 0, x: 30), center (opacity: 1, x: 0), exit (opacity: 0, x: -30). Transition duration: 0.25s.
    - Render the appropriate Step component based on currentStepIndex.
    - goNext/goBack functions that increment/decrement currentStepIndex.

    StepReview.tsx:
    - Props: { slot: SlotWithTrainer; notes: string; onNotesChange: (v: string) => void; onNext: () => void; }
    - Display trainer info (name, avatar, specialty), session date/time, duration. Notes textarea. "Continue" button calling onNext. "Back to trainer" link to /trainers/:id.
    - Extract the review step rendering from BookSession.tsx (~lines 430-530).

    StepConfirm.tsx:
    - Props: { slot: SlotWithTrainer; notes: string; onConfirm: () => Promise<void>; onBack: () => void; loading: boolean; }
    - Display pricing breakdown: base rate, discount (if any), total. Session summary. "Confirm Booking" button calling onConfirm. "Back" button calling onBack.
    - Extract the confirm step rendering from BookSession.tsx.

    StepPayment.tsx:
    - Props: { clientSecret: string; amount: number; onSuccess: () => void; onBack: () => void; }
    - Wraps existing PaymentForm in Stripe Elements provider. Extract the payment step from BookSession.tsx.
    - Keep the existing PaymentForm component logic intact (it handles stripe.confirmPayment).

    StepSuccess.tsx:
    - Props: { trainerName: string; sessionDate: string; }
    - Display confirmation with Check icon, session summary, "View My Bookings" link to /client/bookings, "Browse More Trainers" link to /.
    - Extract the success step rendering from BookSession.tsx.

    Follow the exact ProgressIndicator and animation patterns from 20-RESEARCH.md Pattern 1.
    Accept browser back behavior as-is (option b from Pitfall 4) -- do NOT use useSearchParams for step tracking.
  </action>
  <verify>
    <automated>cd "Cenlar demand gt 1-17" && npx vitest run src/components/booking/BookingWizard.test.tsx --reporter=verbose</automated>
  </verify>
  <done>BookingWizard renders progress indicator with dynamic step count. AnimatePresence handles step transitions. All 4 step components render their content correctly. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor BookSession.tsx to use BookingWizard</name>
  <files>src/pages/BookSession.tsx</files>
  <action>
    Refactor BookSession.tsx from 628 lines to ~150-200 lines by:

    1. Keep the data-fetching logic (useEffect that loads slot data with trainer profile join, ~lines 100-175).
    2. Keep the booking creation logic (handleBooking function that creates the booking record and optionally creates a Stripe payment intent, ~lines 175-275).
    3. Keep the PaymentForm component definition at the top of the file (it uses useStripe/useElements hooks that require it to be inside Elements provider, which StepPayment handles).
    4. Remove ALL inline step rendering JSX (the large switch/conditional blocks for review/confirm/payment/success).
    5. Replace with: after loading completes and slotData exists, render `<BookingWizard slot={slotData} onComplete={() => navigate('/client/bookings')} stripeConfigured={STRIPE_CONFIGURED} />`.
    6. Move the booking creation and payment intent logic into BookingWizard or pass as callbacks. The key functions to preserve: handleBooking (creates booking in DB, returns booking ID), createPaymentIntent (calls Edge Function, returns client_secret).
    7. For the loading state: Use SkeletonRect + SkeletonLine from Plan 01 instead of the spinner (already handled in Plan 02, but if Plan 03 executes first, add it here).
    8. For the error state: Use ErrorState from Plan 01 with backTo pointing to "/" if slot loading fails.

    The refactored BookSession.tsx should have this structure:
    - Imports
    - PaymentForm component (kept as-is, ~60 lines)
    - BookSession page component:
      - useParams, useNavigate, useAuthStore
      - slotData state + loading + error
      - useEffect to fetch slot data
      - handleBooking callback
      - createPaymentIntent callback
      - Return: loading skeleton | error state | BookingWizard

    Pass handleBooking and createPaymentIntent as props to BookingWizard (add them to BookingWizard's props interface). BookingWizard calls handleBooking when user confirms on StepConfirm, then either advances to StepPayment (if Stripe) or StepSuccess (if no Stripe).

    CRITICAL: Preserve all existing booking logic exactly. This is a refactor for UI structure, not a rewrite of business logic. The booking creation, payment intent, and Stripe confirmation flows must work identically.
  </action>
  <verify>
    <automated>cd "Cenlar demand gt 1-17" && npx vitest run --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>BookSession.tsx reduced from 628 lines to ~200 lines. All booking logic preserved. Progress indicator visible. Step transitions animated. Payment flow works identically. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
cd "Cenlar demand gt 1-17" && npx vitest run --reporter=verbose && npx tsc --noEmit
</verification>

<success_criteria>
- Booking flow shows numbered progress indicator (3 or 4 steps depending on Stripe)
- Steps transition with smooth slide animation via Framer Motion
- Each step is a focused component (Review, Confirm, Payment, Success)
- Pricing breakdown clearly visible on confirm step
- Back navigation works between steps
- BookSession.tsx reduced from 628 to ~200 lines
- All existing booking/payment logic preserved
- TypeScript compiles with no errors
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/20-ux-polish/20-03-SUMMARY.md`
</output>
