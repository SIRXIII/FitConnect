---
phase: 20-ux-polish
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - src/components/skeleton/TrainerCardSkeleton.tsx
  - src/components/skeleton/ProfileSkeleton.tsx
  - src/components/skeleton/BookingCardSkeleton.tsx
  - src/components/search/TrainerCard.tsx
  - src/components/search/SearchSection.tsx
  - src/components/shared/ErrorBoundary.tsx
  - src/components/landing/BestDeals.tsx
  - src/components/landing/FeaturedTrainers.tsx
  - src/components/landing/ReferralLeaderboard.tsx
  - src/pages/TrainerProfile.tsx
  - src/pages/BookSession.tsx
  - src/pages/ClientPassport.tsx
  - src/pages/MyBookings.tsx
  - src/pages/TrainerBookings.tsx
  - src/pages/Messages.tsx
  - src/pages/ClientDashboard.tsx
autonomous: true
requirements: [UXP-02, UXP-03, UXP-04]

must_haves:
  truths:
    - "Trainer search shows skeleton cards while loading instead of a spinner"
    - "TrainerProfile page shows content-shaped skeleton while loading"
    - "All img tags have loading=lazy and decoding=async attributes"
    - "Unsplash fallback URLs include width/quality/format optimization"
    - "ErrorBoundary renders ErrorState instead of raw error.message"
    - "Pages with data loading failures show ErrorState with retry buttons"
  artifacts:
    - path: "src/components/skeleton/TrainerCardSkeleton.tsx"
      provides: "Skeleton matching TrainerCard layout (aspect-[4/5] image + text lines)"
      exports: ["TrainerCardSkeleton"]
    - path: "src/components/skeleton/ProfileSkeleton.tsx"
      provides: "Skeleton matching TrainerProfile layout"
      exports: ["ProfileSkeleton"]
    - path: "src/components/skeleton/BookingCardSkeleton.tsx"
      provides: "Skeleton matching booking card layout"
      exports: ["BookingCardSkeleton"]
  key_links:
    - from: "src/components/search/SearchSection.tsx"
      to: "src/components/skeleton/TrainerCardSkeleton.tsx"
      via: "renders 6x TrainerCardSkeleton when loading"
      pattern: "TrainerCardSkeleton"
    - from: "src/components/shared/ErrorBoundary.tsx"
      to: "src/components/shared/ErrorState.tsx"
      via: "renders ErrorState in error state"
      pattern: "ErrorState"
    - from: "src/components/search/TrainerCard.tsx"
      to: "src/lib/imageUtils.ts"
      via: "wraps img src with optimizedUrl"
      pattern: "optimizedUrl"
---

<objective>
Apply skeleton loading screens, image optimization, and actionable error states across the entire app. Replace all content-loading spinners with content-shaped skeletons, add lazy loading to all images, optimize Unsplash URLs, and integrate ErrorState into error-handling code paths.

Purpose: This is the cross-cutting integration pass that delivers the visible UX improvements to users across every page.
Output: 3 new skeleton components, 16 files updated with skeletons/lazy images/error states.
</objective>

<execution_context>
@/Users/xfiles/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xfiles/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-ux-polish/20-RESEARCH.md
@.planning/phases/20-ux-polish/20-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->
From src/components/shared/Skeleton.tsx:
```typescript
export const SkeletonLine: React.FC<{ width?: string; className?: string }>;
export const SkeletonCircle: React.FC<{ size?: string }>;
export const SkeletonRect: React.FC<{ className?: string }>;
```

From src/components/shared/ErrorState.tsx:
```typescript
export const ErrorState: React.FC<{
  title?: string;
  message: string;
  onRetry?: () => void;
  backTo?: { label: string; path: string };
  icon?: React.ReactNode;
}>;
```

From src/lib/errorMessages.ts:
```typescript
export interface AppError { title: string; message: string; recoverable: boolean; }
export function mapError(error: unknown): AppError;
```

From src/lib/imageUtils.ts:
```typescript
export function optimizedUrl(url: string, width?: number): string;
export const IMAGE_ATTRS: { loading: 'lazy'; decoding: 'async' };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create page-specific skeleton components and replace spinners across the app</name>
  <files>
    src/components/skeleton/TrainerCardSkeleton.tsx,
    src/components/skeleton/ProfileSkeleton.tsx,
    src/components/skeleton/BookingCardSkeleton.tsx,
    src/components/search/SearchSection.tsx,
    src/pages/TrainerProfile.tsx,
    src/pages/BookSession.tsx,
    src/pages/ClientPassport.tsx,
    src/pages/MyBookings.tsx,
    src/pages/TrainerBookings.tsx,
    src/pages/Messages.tsx,
    src/pages/ClientDashboard.tsx,
    src/components/landing/BestDeals.tsx
  </files>
  <action>
    Create 3 skeleton components using the Skeleton primitives from Plan 01:

    1. TrainerCardSkeleton.tsx: Match TrainerCard's exact layout -- SkeletonRect with aspect-[4/5], then space-y-4 containing SkeletonLine w-36 h-6 + SkeletonLine w-24 h-3 on left, SkeletonLine w-16 h-6 on right, then SkeletonRect h-12 at bottom. Import from "@/components/shared/Skeleton".

    2. ProfileSkeleton.tsx: Match TrainerProfile's layout -- large SkeletonRect aspect-[3/4] for photo, then space-y-4 with SkeletonLine w-48 h-8 (name), SkeletonLine w-32 h-4 (specialty), SkeletonRect h-24 (bio area), and a grid of 3 SkeletonRect h-16 for stat cards.

    3. BookingCardSkeleton.tsx: Match MyBookings card layout -- horizontal flex with SkeletonCircle w-12 h-12, then space-y-2 with SkeletonLine w-40 h-5 + SkeletonLine w-24 h-3 + SkeletonLine w-32 h-3.

    Then replace spinner instances in these files (replace the animate-spin div with the appropriate skeleton):

    - SearchSection.tsx (~line 154): Replace spinner with grid of 6x TrainerCardSkeleton in same grid layout as results
    - TrainerProfile.tsx (~line 222-227): Replace full-page spinner with ProfileSkeleton
    - TrainerProfile.tsx (~line 408-410): Replace slots spinner with 3x SkeletonRect h-12
    - BookSession.tsx (~line 296-300): Replace spinner with SkeletonRect h-64 + SkeletonLine w-48 h-6 (simple form skeleton)
    - ClientPassport.tsx (~line 189-193): Replace spinner with SkeletonCircle w-20 h-20 + 4x SkeletonLine
    - MyBookings.tsx (~line 300): Replace spinner with 3x BookingCardSkeleton
    - TrainerBookings.tsx (~line 271): Replace spinner with 3x BookingCardSkeleton
    - Messages.tsx (~line 253 and ~line 355): Replace spinners with 4x SkeletonLine w-full h-16
    - ClientDashboard.tsx: Add 3x SkeletonRect h-20 for stat cards if loading state exists
    - BestDeals.tsx (~line 148): Replace spinner with 3x TrainerCardSkeleton in a horizontal scroll

    KEEP spinners for transient submit/action states (form submission buttons, ClientOnboarding step submit). Only replace content-loading spinners.

    Do NOT touch Login.tsx, AuthCallback.tsx, ClientOnboarding.tsx, RoleSelect.tsx, TrainerOnboarding.tsx, AvailabilityManager.tsx, or ProtectedRoute.tsx spinners -- those are transient auth/submit states where spinners are appropriate.
  </action>
  <verify>
    <automated>cd "Cenlar demand gt 1-17" && npx vitest run --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>All content-loading spinners replaced with content-shaped skeletons. Skeleton components match the dimensions of their real content counterparts. TypeScript compiles with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add image optimization and integrate ErrorState across the app</name>
  <files>
    src/components/search/TrainerCard.tsx,
    src/components/search/SearchSection.tsx,
    src/components/shared/ErrorBoundary.tsx,
    src/components/landing/FeaturedTrainers.tsx,
    src/components/landing/ReferralLeaderboard.tsx,
    src/pages/TrainerProfile.tsx,
    src/pages/BookSession.tsx,
    src/pages/MyBookings.tsx,
    src/pages/TrainerBookings.tsx,
    src/pages/Messages.tsx
  </files>
  <action>
    IMAGE OPTIMIZATION (UXP-02):

    Add `loading="lazy" decoding="async"` to every img tag in these files. Wrap Unsplash source URLs with `optimizedUrl()` from "@/lib/imageUtils":

    - TrainerCard.tsx (line 17): Add loading="lazy" decoding="async" to the img tag. Wrap `trainer.imageUrl` with `optimizedUrl(trainer.imageUrl)`.
    - SearchSection.tsx (line 26): Wrap the Unsplash fallback URL in the imageUrl mapping with optimizedUrl().
    - FeaturedTrainers.tsx: Add loading="lazy" decoding="async" to avatar img tags.
    - ReferralLeaderboard.tsx (line 75): Add loading="lazy" decoding="async" to avatar img tag.
    - TrainerProfile.tsx: Add loading="lazy" decoding="async" to trainer avatar and review avatar img tags.
    - BookSession.tsx: Add loading="lazy" decoding="async" to trainer avatar img tag (~line 484).
    - MyBookings.tsx: Add loading="lazy" decoding="async" to trainer avatar img tag (~line 338).
    - TrainerBookings.tsx: Add loading="lazy" decoding="async" to client avatar img tag (~line 297).
    - Messages.tsx: Add loading="lazy" decoding="async" to participant avatar img tags (~lines 278, 333).

    ERROR STATE INTEGRATION (UXP-04):

    - ErrorBoundary.tsx: Import ErrorState from "@/components/shared/ErrorState". In the render error block, replace the existing inline error display with `<ErrorState title="Something went wrong" message="An unexpected error occurred. Please refresh the page to try again." onRetry={() => window.location.reload()} />`. Remove the raw error.message monospace block entirely.

    - TrainerProfile.tsx: Where trainer is not found (error or null trainer after load), render `<ErrorState title="Trainer not found" message="This trainer profile could not be loaded." onRetry={refetch} backTo={{ label: "Browse Trainers", path: "/" }} />` instead of any existing error display.

    - BookSession.tsx: Where slot loading fails, render `<ErrorState message="Could not load booking details. The session may no longer be available." backTo={{ label: "Browse Trainers", path: "/" }} />`.

    - MyBookings.tsx, TrainerBookings.tsx: Where bookings fail to load, add ErrorState with onRetry pointing to the fetch function. Use `mapError(error)` from "@/lib/errorMessages" to derive title and message.

    - Messages.tsx: Where conversations fail to load, add ErrorState with onRetry. Use mapError for message derivation.

    For all ErrorState retry handlers: clear the error state, set loading to true, then call the fetch function (per Pitfall 3 from RESEARCH.md).
  </action>
  <verify>
    <automated>cd "Cenlar demand gt 1-17" && npx vitest run --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>All img tags have loading="lazy" and decoding="async". Unsplash URLs wrapped with optimizedUrl. ErrorBoundary uses ErrorState. Data-loading error paths in TrainerProfile, BookSession, MyBookings, TrainerBookings, and Messages show ErrorState with retry/back actions instead of raw error strings.</done>
</task>

</tasks>

<verification>
cd "Cenlar demand gt 1-17" && npx vitest run --reporter=verbose && npx tsc --noEmit
</verification>

<success_criteria>
- Zero animate-spin instances in content-loading contexts (transient submit spinners are fine)
- All img tags have loading="lazy" decoding="async" attributes
- Unsplash URLs optimized with width/quality/format parameters
- ErrorBoundary renders ErrorState instead of raw error.message
- 5+ pages have ErrorState with retry/back navigation for data loading failures
- TypeScript compiles with no errors
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/20-ux-polish/20-02-SUMMARY.md`
</output>
