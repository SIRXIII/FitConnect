---
phase: 18-trainee-fitness-passport
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cenlar demand gt 1-17/supabase/migrations/20260317300000_fitness_passport.sql
autonomous: true
requirements:
  - FIT-02
  - FIT-04

must_haves:
  truths:
    - "client_profiles table has bio and training_frequency columns"
    - "Existing data is preserved (migration is additive)"
  artifacts:
    - path: "Cenlar demand gt 1-17/supabase/migrations/20260317300000_fitness_passport.sql"
      provides: "DB migration adding bio + training_frequency columns"
      contains: "ALTER TABLE public.client_profiles"
  key_links:
    - from: "client_profiles table"
      to: "fitnessPassportSchema in src/lib/schemas.ts"
      via: "column names match Zod field names"
      pattern: "bio|training_frequency"
---

<objective>
Add bio and training_frequency columns to client_profiles table via Supabase migration.

Purpose: The existing client_profiles table (from 20260315120000_onboarding.sql) has fitness_goals[], workout_types[], health_notes, but is missing `bio` (text) and `training_frequency` (text) which the fitnessPassportSchema in schemas.ts already validates. This migration closes the DB gap so plans 18-02 and 18-03 can read/write these fields.

Output: One migration file ready for `supabase db push`.
</objective>

<execution_context>
@/Users/xfiles/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xfiles/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@Cenlar demand gt 1-17/supabase/migrations/20260315120000_onboarding.sql
@Cenlar demand gt 1-17/src/lib/schemas.ts

<interfaces>
<!-- Existing client_profiles columns (from onboarding migration): -->
<!-- id, user_id, age, weight_lbs, height_ft, height_in, body_type, fitness_level, -->
<!-- fitness_goals[], workout_types[], preferred_session_length, health_notes, -->
<!-- stripe_customer_id, stripe_payment_method_id, stripe_payment_last4, stripe_payment_brand -->

<!-- fitnessPassportSchema expects these fields: -->
<!-- fitness_goals: array of enums, workout_types: array of enums, -->
<!-- training_frequency: enum ('1-2', '3-4', '5-6', '7+'), -->
<!-- physical_limitations: string max 1000, bio: string max 500 -->

<!-- Note: physical_limitations maps to existing health_notes column (reuse, don't duplicate) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration adding bio and training_frequency columns</name>
  <files>Cenlar demand gt 1-17/supabase/migrations/20260317300000_fitness_passport.sql</files>
  <action>
Create a SQL migration file that:

1. Adds `bio` column (text, nullable, max 500 chars via CHECK constraint) to client_profiles
2. Adds `training_frequency` column (text, nullable, CHECK constraint for values '1-2', '3-4', '5-6', '7+') to client_profiles
3. Adds a comment explaining these support the Fitness Passport feature (Phase 18)

Do NOT add physical_limitations — the existing `health_notes` column serves that purpose (same data, different label in UI).

Do NOT modify RLS policies — the existing "Users manage own client profile" (FOR ALL) and "Trainers can view client profiles for their bookings" (FOR SELECT) already cover these new columns.

Migration naming convention: follows existing pattern (YYYYMMDDHHMMSS_descriptive_name.sql). Use timestamp 20260317300000 to sort after the audit_log migration (20260317200000).
  </action>
  <verify>
    <automated>cd "Cenlar demand gt 1-17" && grep -c "ALTER TABLE" supabase/migrations/20260317300000_fitness_passport.sql && grep "training_frequency" supabase/migrations/20260317300000_fitness_passport.sql && grep "bio" supabase/migrations/20260317300000_fitness_passport.sql</automated>
  </verify>
  <done>Migration file exists with ALTER TABLE statements adding bio (text, max 500) and training_frequency (text, enum check) to client_profiles. No RLS changes, no destructive operations.</done>
</task>

</tasks>

<verification>
- Migration file parses as valid SQL (no syntax errors)
- Only additive changes (ALTER TABLE ADD COLUMN IF NOT EXISTS)
- CHECK constraints match fitnessPassportSchema validation rules
</verification>

<success_criteria>
Migration file at `Cenlar demand gt 1-17/supabase/migrations/20260317300000_fitness_passport.sql` contains two ALTER TABLE ADD COLUMN statements with appropriate CHECK constraints matching the Zod schema.
</success_criteria>

<output>
After completion, create `.planning/phases/18-trainee-fitness-passport/18-01-SUMMARY.md`
</output>
