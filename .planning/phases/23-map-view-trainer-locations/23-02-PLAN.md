---
phase: 23-map-view-trainer-locations
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - Cenlar demand gt 1-17/src/hooks/useWorkoutLocations.ts
  - Cenlar demand gt 1-17/src/components/trainer/WorkoutLocationsManager.tsx
  - Cenlar demand gt 1-17/src/components/trainer/GoLiveLocationPicker.tsx
  - Cenlar demand gt 1-17/src/components/trainer/AvailabilityHeader.tsx
  - Cenlar demand gt 1-17/src/hooks/useAvailabilitySession.ts
  - Cenlar demand gt 1-17/src/pages/TrainerProfile.tsx
autonomous: true
requirements: [LOC-01, LOC-02, LOC-03, LOC-04]

must_haves:
  truths:
    - "Trainer can add a workout location with address autocomplete and see it on a map preview"
    - "Trainer can drag the pin to fine-tune position after address selection"
    - "Trainer can select gym, park, or in-home as location type"
    - "Trainer can manage up to 5 workout locations with nicknames"
    - "Trainer sees location picker when going live and selected location becomes the active pin"
  artifacts:
    - path: "Cenlar demand gt 1-17/src/hooks/useWorkoutLocations.ts"
      provides: "CRUD operations for workout_locations table"
      exports: ["useWorkoutLocations"]
    - path: "Cenlar demand gt 1-17/src/components/trainer/WorkoutLocationsManager.tsx"
      provides: "Trainer settings UI for managing workout locations"
      exports: ["WorkoutLocationsManager"]
    - path: "Cenlar demand gt 1-17/src/components/trainer/GoLiveLocationPicker.tsx"
      provides: "Modal location picker in Go Live flow"
      exports: ["GoLiveLocationPicker"]
  key_links:
    - from: "WorkoutLocationsManager"
      to: "useWorkoutLocations"
      via: "hook provides locations array and CRUD methods"
      pattern: "useWorkoutLocations"
    - from: "GoLiveLocationPicker"
      to: "useWorkoutLocations"
      via: "reads saved locations for picker display"
      pattern: "useWorkoutLocations"
    - from: "AvailabilityHeader"
      to: "GoLiveLocationPicker"
      via: "shows picker before calling goLive with locationId"
      pattern: "GoLiveLocationPicker"
    - from: "useAvailabilitySession.goLive"
      to: "trainer_profiles.active_location_id"
      via: "goLive writes active_location_id to DB"
      pattern: "active_location_id"
---

<objective>
Build the trainer location management system: CRUD hook for workout locations, address autocomplete UI with drag-to-adjust pin, location type selection, and the Go Live location picker that writes active_location_id to trainer_profiles.

Purpose: Trainers need to save workout locations before clients can see them on the map. The Go Live flow needs a location picker so the trainer's pin appears at the right spot.
Output: useWorkoutLocations hook, WorkoutLocationsManager component in trainer settings, GoLiveLocationPicker in AvailabilityHeader flow.
</objective>

<execution_context>
@/Users/xfiles/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xfiles/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-map-view-trainer-locations/23-CONTEXT.md
@.planning/phases/23-map-view-trainer-locations/23-RESEARCH.md
@.planning/phases/23-map-view-trainer-locations/23-UI-SPEC.md
@.planning/phases/23-map-view-trainer-locations/23-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/types/map.ts:
```typescript
export type LocationType = 'gym' | 'park' | 'in-home';
export interface WorkoutLocation {
  id: string; trainer_id: string; nickname: string | null;
  address: string; latitude: number; longitude: number;
  location_type: LocationType; created_at: string; updated_at: string;
}
export const PIN_COLORS: Record<LocationType, string>;
export const LA_DEFAULT: { lat: number; lng: number };
```

From src/hooks/useAvailabilitySession.ts:
```typescript
export function useAvailabilitySession(trainerProfile: TrainerProfile | null): {
  goLive: (bookingMode: 'instant' | 'request', timer: number | 'eod' | null) => void;
  // ...other fields
};
```

From src/components/trainer/AvailabilityHeader.tsx:
```typescript
// goLive called at line 74: goLive(pendingBookingMode, pendingTimer);
// Need to add locationId parameter
```

From src/stores/auth.ts (TrainerProfile):
```typescript
// trainer_profiles Row type - includes active_location_id: string | null (added in Plan 01)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useWorkoutLocations hook and WorkoutLocationsManager</name>
  <files>
    Cenlar demand gt 1-17/src/hooks/useWorkoutLocations.ts,
    Cenlar demand gt 1-17/src/components/trainer/WorkoutLocationsManager.tsx,
    Cenlar demand gt 1-17/src/pages/TrainerProfile.tsx
  </files>
  <read_first>
    Cenlar demand gt 1-17/src/types/map.ts,
    Cenlar demand gt 1-17/src/types/supabase.ts,
    Cenlar demand gt 1-17/src/lib/supabase.ts,
    Cenlar demand gt 1-17/src/stores/auth.ts,
    Cenlar demand gt 1-17/src/pages/TrainerProfile.tsx,
    .planning/phases/23-map-view-trainer-locations/23-UI-SPEC.md,
    .planning/phases/23-map-view-trainer-locations/23-RESEARCH.md
  </read_first>
  <action>
1. Create `src/hooks/useWorkoutLocations.ts`:
   - Import `supabase` from `@/lib/supabase` and `WorkoutLocation`, `LocationType` from `@/types/map`
   - State: `locations: WorkoutLocation[]`, `loading: boolean`, `error: string | null`
   - `fetchLocations(trainerId: string)`: SELECT from `workout_locations` WHERE `trainer_id = trainerId` ORDER BY `created_at`
   - `addLocation(trainerId, data: { address: string; nickname?: string; latitude: number; longitude: number; location_type: LocationType })`:
     - INSERT into `workout_locations` with `trainer_id` and data fields, `.select().single()`
     - Handle error code `P0001` (raise exception from check_location_limit trigger) — show "Maximum 5 workout locations allowed"
     - Refetch after success
   - `updateLocation(id: string, data: Partial<{ nickname: string; latitude: number; longitude: number; location_type: LocationType }>)`:
     - UPDATE `workout_locations` WHERE `id = id`, `.select().single()`
     - Refetch after success
   - `deleteLocation(id: string)`:
     - DELETE from `workout_locations` WHERE `id = id`
     - Refetch after success
   - `canAddMore`: computed boolean `locations.length < 5`
   - Return `{ locations, loading, error, addLocation, updateLocation, deleteLocation, canAddMore, refetch: fetchLocations }`

2. Create `src/components/trainer/WorkoutLocationsManager.tsx`:
   - Import `APIProvider, Map, AdvancedMarker, useMapsLibrary` from `@vis.gl/react-google-maps`
   - Import `Dumbbell, Leaf, Home, Trash2, MapPin, Plus` from `lucide-react`
   - Import `useWorkoutLocations` from `@/hooks/useWorkoutLocations`
   - Import `WorkoutLocation, LocationType, PIN_COLORS, LA_DEFAULT` from `@/types/map`
   - Props: `trainerId: string`
   - States: `isAdding: boolean`, `editAddress: string`, `editNickname: string`, `editType: LocationType | null`, `editCoords: {lat, lng} | null`, `suggestions: PlaceSuggestion[]`, `deleteConfirmId: string | null`
   - Address input: uses `useMapsLibrary('places')` to get Places library, then on input change (debounced, min 3 chars) calls `AutocompleteSuggestion.fetchAutocompleteSuggestions({ input: editAddress })` — renders suggestion list below input
   - On suggestion select: calls `suggestion.placePrediction.toPlace()` then `.fetchFields({ fields: ['location', 'formattedAddress'] })`, extracts lat/lng/address, sets `editCoords` and `editAddress`
   - Map preview: small `<Map>` (height 200px) centered on `editCoords` with a draggable `<AdvancedMarker>` — `onDragEnd` updates `editCoords` with new position
   - Location type toggle: three buttons (Gym/Park/In-Home) with icons (`Dumbbell`/`Leaf`/`Home`), selected state uses `PIN_COLORS[type]` background with white text, unselected uses `border-ink/10` background
   - Nickname input: optional, placeholder "e.g. Venice Beach Spot", max 40 chars (`maxLength={40}`)
   - Save button: calls `addLocation(trainerId, { address: editAddress, nickname: editNickname || null, latitude: editCoords.lat, longitude: editCoords.lng, location_type: editType })` — disabled until `editCoords` and `editType` are set
   - Saved locations list: each card shows location icon (colored by type) + nickname/address + type label + `Trash2` delete button
   - Delete flow: clicking Trash2 sets `deleteConfirmId`, shows inline confirmation with copy "Remove this location? This cannot be undone." and Cancel/Remove buttons (Remove in `text-red-500`)
   - "Add Location" button: disabled with tooltip "Maximum 5 locations reached" when `!canAddMore`
   - Empty state: "No locations saved. Add your first workout spot."
   - Section heading: `text-[10px] uppercase tracking-[0.2em] font-semibold text-ink/40` label "WORKOUT LOCATIONS"
   - Wrap entire component in `<APIProvider apiKey={import.meta.env.VITE_GOOGLE_MAPS_API_KEY} libraries={['places']}>`

3. Add `WorkoutLocationsManager` to `TrainerProfile.tsx`:
   - Import `WorkoutLocationsManager` from `@/components/trainer/WorkoutLocationsManager`
   - Find the existing settings/profile section in TrainerProfile.tsx
   - Add `<WorkoutLocationsManager trainerId={trainerProfile.id} />` section after the existing profile fields
   - Guard: only render when `trainerProfile?.id` exists
  </action>
  <verify>
    <automated>cd "Cenlar demand gt 1-17" && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useWorkoutLocations.ts` exports `useWorkoutLocations`
    - `useWorkoutLocations.ts` contains `supabase.from('workout_locations')` calls for INSERT, UPDATE, DELETE, SELECT
    - `useWorkoutLocations.ts` contains `locations.length < 5` for `canAddMore`
    - `WorkoutLocationsManager.tsx` contains `APIProvider` and `useMapsLibrary('places')`
    - `WorkoutLocationsManager.tsx` contains `AutocompleteSuggestion.fetchAutocompleteSuggestions`
    - `WorkoutLocationsManager.tsx` contains `draggable={true}` on an AdvancedMarker
    - `WorkoutLocationsManager.tsx` contains location type buttons for `gym`, `park`, `in-home`
    - `WorkoutLocationsManager.tsx` contains `maxLength={40}` on nickname input
    - `WorkoutLocationsManager.tsx` contains "Maximum 5 locations reached" tooltip text
    - `WorkoutLocationsManager.tsx` contains "Remove this location? This cannot be undone." confirmation text
    - `TrainerProfile.tsx` renders `WorkoutLocationsManager`
    - All existing tests pass
  </acceptance_criteria>
  <done>Trainers can add workout locations with Places Autocomplete address entry, drag-to-adjust pin, location type selection (gym/park/in-home), optional nickname, manage up to 5 locations, and delete with confirmation. Component is integrated in trainer profile settings page.</done>
</task>

<task type="auto">
  <name>Task 2: GoLiveLocationPicker and AvailabilityHeader integration</name>
  <files>
    Cenlar demand gt 1-17/src/components/trainer/GoLiveLocationPicker.tsx,
    Cenlar demand gt 1-17/src/components/trainer/AvailabilityHeader.tsx,
    Cenlar demand gt 1-17/src/hooks/useAvailabilitySession.ts
  </files>
  <read_first>
    Cenlar demand gt 1-17/src/components/trainer/AvailabilityHeader.tsx,
    Cenlar demand gt 1-17/src/hooks/useAvailabilitySession.ts,
    Cenlar demand gt 1-17/src/hooks/useWorkoutLocations.ts,
    Cenlar demand gt 1-17/src/types/map.ts,
    Cenlar demand gt 1-17/src/stores/auth.ts,
    .planning/phases/23-map-view-trainer-locations/23-UI-SPEC.md
  </read_first>
  <action>
1. Modify `useAvailabilitySession.ts`:
   - Change `goLive` signature to accept optional `locationId`:
     ```typescript
     goLive: (bookingMode: 'instant' | 'request', timer: number | 'eod' | null, locationId?: string) => void;
     ```
   - In the `goLive` callback, add `active_location_id: locationId ?? null` to the supabase `.update()` call on `trainer_profiles` (line ~143):
     ```typescript
     const { error } = await supabase
       .from('trainer_profiles')
       .update({
         availability_status: 'live',
         booking_mode: mode,
         sleep_timer_expires_at: sleepTimerExpiresAt,
         availability_session_started_at: new Date().toISOString(),
         active_location_id: locationId ?? null,
       })
       .eq('id', trainerProfile.id);
     ```
   - In `goOffline`, also clear `active_location_id: null` in the update call (line ~171)

2. Create `src/components/trainer/GoLiveLocationPicker.tsx`:
   - Import `Dumbbell, Leaf, Home, MapPin` from `lucide-react`
   - Import `useWorkoutLocations` from `@/hooks/useWorkoutLocations`
   - Import `WorkoutLocation, LocationType, PIN_COLORS` from `@/types/map`
   - Props: `trainerId: string; onSelect: (locationId: string) => void; onClose: () => void`
   - Fetch locations with `useWorkoutLocations` (pass `trainerId`)
   - Render as a modal overlay (fixed inset-0 z-[60] bg-black/50 backdrop-blur-sm)
   - Modal content: centered card (max-w-sm) with white background
   - Heading: "Where are you training today?" in `text-[10px] uppercase tracking-[0.2em] font-semibold text-ink/40`
   - List of saved locations as selectable cards:
     - Each card: location type icon (Dumbbell/Leaf/Home colored by PIN_COLORS), nickname or address (truncated), location_type label
     - Selected card gets `border-accent` highlight
     - Clicking a card selects it (controlled state: `selectedId`)
   - CTA button: "Go Live Here" — calls `onSelect(selectedId)`, disabled until a location is selected
   - No locations state: "Add a workout location first" with a link to trainer settings (`/trainer/profile`)
   - Close button: X in top-right, calls `onClose`
   - Body scroll lock while modal is open

3. Modify `AvailabilityHeader.tsx`:
   - Import `GoLiveLocationPicker` from `@/components/trainer/GoLiveLocationPicker`
   - Add state: `const [showLocationPicker, setShowLocationPicker] = useState(false);`
   - Add state: `const [pendingLocationId, setPendingLocationId] = useState<string | null>(null);`
   - Modify `handleToggle` for the `uiStatus === 'offline'` branch:
     - Instead of directly calling `goLive(pendingBookingMode, pendingTimer)`, call `setShowLocationPicker(true)`
   - Add `handleLocationSelect` callback:
     ```typescript
     const handleLocationSelect = (locationId: string) => {
       setPendingLocationId(locationId);
       setShowLocationPicker(false);
       goLive(pendingBookingMode, pendingTimer, locationId);
       dismissTooltip();
     };
     ```
   - Render `GoLiveLocationPicker` conditionally:
     ```tsx
     {showLocationPicker && trainerProfile && (
       <GoLiveLocationPicker
         trainerId={trainerProfile.id}
         onSelect={handleLocationSelect}
         onClose={() => setShowLocationPicker(false)}
       />
     )}
     ```
  </action>
  <verify>
    <automated>cd "Cenlar demand gt 1-17" && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `useAvailabilitySession.ts` goLive signature includes `locationId?: string` parameter
    - `useAvailabilitySession.ts` goLive update includes `active_location_id: locationId ?? null`
    - `useAvailabilitySession.ts` goOffline update includes `active_location_id: null`
    - `GoLiveLocationPicker.tsx` exports `GoLiveLocationPicker`
    - `GoLiveLocationPicker.tsx` contains "Where are you training today?" heading text
    - `GoLiveLocationPicker.tsx` contains "Go Live Here" button text
    - `GoLiveLocationPicker.tsx` contains "Add a workout location first" empty state text
    - `AvailabilityHeader.tsx` imports and renders `GoLiveLocationPicker`
    - `AvailabilityHeader.tsx` contains `showLocationPicker` state
    - `AvailabilityHeader.tsx` calls `goLive` with three arguments including locationId
    - All existing tests pass
  </acceptance_criteria>
  <done>GoLiveLocationPicker shows saved locations when trainer taps Go Live. Selecting a location and tapping "Go Live Here" passes locationId to goLive, which writes active_location_id to trainer_profiles. Going offline clears active_location_id.</done>
</task>

</tasks>

<verification>
- `useWorkoutLocations` hook provides CRUD for workout_locations table
- `WorkoutLocationsManager` renders in trainer profile settings with Places Autocomplete, drag pin, type selector, max-5 enforcement
- `GoLiveLocationPicker` shows when trainer goes live, writes active_location_id
- `goLive` and `goOffline` both manage active_location_id on trainer_profiles
- `npx vitest run` full suite green
</verification>

<success_criteria>
- Trainer can add locations with address entry, adjust pin, select type, set nickname
- Max 5 locations enforced in UI (button disabled) and DB (trigger)
- Go Live flow shows location picker; selected location ID persists as active_location_id
- Going offline clears active_location_id
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/23-map-view-trainer-locations/23-02-SUMMARY.md`
</output>
