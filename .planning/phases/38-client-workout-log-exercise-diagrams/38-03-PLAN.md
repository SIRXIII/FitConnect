---
phase: 38-client-workout-log-exercise-diagrams
plan: 03
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - "Cenlar demand gt 1-17/src/components/shared/ClientWorkoutSummary.tsx"
  - "Cenlar demand gt 1-17/src/pages/TrainerBookings.tsx"
  - "Cenlar demand gt 1-17/src/pages/ClientPassport.tsx"
autonomous: true
requirements: [LOG-05, LOG-06]
must_haves:
  truths:
    - "Trainer can view a client's last 5 workout sessions on the booking detail page"
    - "Trainer can view a client's recent workout logs on the Fitness Passport page"
    - "Each workout session is expandable to show exercises with illustrations, sets, reps, and weight"
    - "Workout summary shows date and exercise count for each session"
  artifacts:
    - path: "Cenlar demand gt 1-17/src/components/shared/ClientWorkoutSummary.tsx"
      provides: "Reusable read-only workout summary showing last 5 sessions"
      min_lines: 60
    - path: "Cenlar demand gt 1-17/src/pages/TrainerBookings.tsx"
      provides: "Updated with ClientWorkoutSummary in booking detail expansion"
      contains: "ClientWorkoutSummary"
    - path: "Cenlar demand gt 1-17/src/pages/ClientPassport.tsx"
      provides: "Updated with ClientWorkoutSummary section"
      contains: "ClientWorkoutSummary"
  key_links:
    - from: "ClientWorkoutSummary.tsx"
      to: "supabase workout_logs"
      via: "select with workout_exercises(*) limited to 5, ordered by logged_at desc"
      pattern: "workout_logs"
    - from: "TrainerBookings.tsx"
      to: "ClientWorkoutSummary.tsx"
      via: "renders ClientWorkoutSummary passing client_id from booking"
      pattern: "ClientWorkoutSummary"
    - from: "ClientPassport.tsx"
      to: "ClientWorkoutSummary.tsx"
      via: "renders ClientWorkoutSummary passing user.id"
      pattern: "ClientWorkoutSummary"
---

<objective>
Build the trainer-facing read-only workout summary and integrate it into TrainerBookings (booking detail) and ClientPassport (Fitness Passport). This gives trainers visibility into a client's recent workout history so they can pick up where the previous trainer left off.

Purpose: The key value proposition of this phase. Trainers see client workout continuity, enabling seamless handoffs between trainers.

Output: ClientWorkoutSummary shared component, updated TrainerBookings and ClientPassport pages.
</objective>

<execution_context>
@/Users/xfiles/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xfiles/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/38-client-workout-log-exercise-diagrams/38-CONTEXT.md
@.planning/phases/38-client-workout-log-exercise-diagrams/38-RESEARCH.md
@.planning/phases/38-client-workout-log-exercise-diagrams/38-01-SUMMARY.md

<interfaces>
<!-- Created by Plan 01, consumed by this plan -->

From Cenlar demand gt 1-17/src/types/workout.ts:
```typescript
export interface SetEntry {
  reps: number;
  weight: number;
  unit: 'lbs' | 'kg';
}

export interface WorkoutExerciseRow {
  id: string;
  log_id: string;
  exercise_name: string;
  exercise_key: string | null;
  sort_order: number;
  sets: SetEntry[];
}

export interface WorkoutLogWithExercises extends WorkoutLogRow {
  workout_exercises: WorkoutExerciseRow[];
}
```

From Cenlar demand gt 1-17/src/lib/workoutUtils.ts:
```typescript
export function formatSet(set: SetEntry): string;
export function formatSetSummary(sets: SetEntry[]): string;
```

From Cenlar demand gt 1-17/src/components/shared/ExerciseDiagram.tsx:
```typescript
// default export
interface ExerciseDiagramProps {
  exerciseKey: string | null;
  muscleGroup?: string;
  size?: number;
  className?: string;
}
```

From Cenlar demand gt 1-17/src/pages/TrainerBookings.tsx (line ~556, existing pattern):
```typescript
{booking.status === 'completed' && (
  <SessionLogPanel
    bookingId={booking.id}
    trainerId={trainerProfile!.id}
    clientId={booking.client_id}
  />
)}
```
The ClientWorkoutSummary will be placed near this SessionLogPanel, showing the client's workout history alongside the trainer's session notes.

From Cenlar demand gt 1-17/src/pages/ClientPassport.tsx (line ~47):
```typescript
const ClientPassport: React.FC = () => {
  const { user, profile, updateProfile } = useAuthStore();
  // ... existing fitness data display
```
The ClientWorkoutSummary will be added as a new section below the existing fitness profile data.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ClientWorkoutSummary shared component</name>
  <files>
    Cenlar demand gt 1-17/src/components/shared/ClientWorkoutSummary.tsx
  </files>
  <read_first>
    Cenlar demand gt 1-17/src/types/workout.ts,
    Cenlar demand gt 1-17/src/lib/workoutUtils.ts,
    Cenlar demand gt 1-17/src/components/shared/ExerciseDiagram.tsx,
    Cenlar demand gt 1-17/src/components/session/SessionNotesDisplay.tsx,
    Cenlar demand gt 1-17/src/lib/supabase.ts
  </read_first>
  <action>
    Create a reusable read-only component that fetches and displays a client's last 5 workout sessions.

    Props: clientId (string), title (optional string, default "Recent Workouts").

    State:
    - logs: WorkoutLogWithExercises[]
    - loading: boolean
    - expandedLogId: string | null

    Fetch logic (useCallback + useEffect):
    - Query (supabase as any).from('workout_logs').select('*, workout_exercises(*)').eq('client_id', clientId).order('logged_at', { ascending: false }).limit(5)
    - Cast result as WorkoutLogWithExercises[]
    - The trainer's RLS policy (created in Plan 01 migration) allows SELECT for clients with whom the trainer has a booking

    Layout:
    - Section header: title prop, Dumbbell icon from lucide-react
    - Loading state: "Loading workout history..." with spinner
    - Empty state: "No workout logs yet." in muted text
    - Log list: each entry is a clickable card showing:
      - Date: formatted as "Mar 23, 2026" using toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' })
      - Exercise count: "{N} exercises" text
      - Notes preview: first 60 characters of notes if present, with ellipsis
      - ChevronDown icon that rotates when expanded (Framer Motion rotate)
    - Expanded view (AnimatePresence accordion, matching SessionNotesDisplay pattern):
      - For each exercise (ordered by sort_order):
        - ExerciseDiagram component (size 32) next to exercise name
        - Below name: each set formatted using formatSet (e.g., "10 x 135lbs"), separated by " | "
      - Full notes text if present, in a muted italic block
    - Styling: Tailwind, border rounded-lg, bg-gray-50 for expanded content, consistent with existing card patterns

    Export as default.
  </action>
  <verify>
    <automated>cd "Cenlar demand gt 1-17" && grep -c "workout_logs" src/components/shared/ClientWorkoutSummary.tsx && grep -c "ExerciseDiagram" src/components/shared/ClientWorkoutSummary.tsx && grep -c "formatSet" src/components/shared/ClientWorkoutSummary.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "clientId" src/components/shared/ClientWorkoutSummary.tsx confirms prop
    - grep "workout_logs" src/components/shared/ClientWorkoutSummary.tsx confirms Supabase query
    - grep "workout_exercises" src/components/shared/ClientWorkoutSummary.tsx confirms nested select
    - grep "limit(5)" src/components/shared/ClientWorkoutSummary.tsx confirms 5-session limit
    - grep "ExerciseDiagram" src/components/shared/ClientWorkoutSummary.tsx confirms illustration rendering
    - grep "formatSet" src/components/shared/ClientWorkoutSummary.tsx confirms set display formatting
    - grep "AnimatePresence" src/components/shared/ClientWorkoutSummary.tsx confirms accordion animation
    - grep "export default" src/components/shared/ClientWorkoutSummary.tsx confirms default export
  </acceptance_criteria>
  <done>
    ClientWorkoutSummary fetches last 5 workout logs for a given client, displays them as expandable cards with exercise illustrations and formatted set details. Works for both trainer-view and client-view contexts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate ClientWorkoutSummary into TrainerBookings and ClientPassport</name>
  <files>
    Cenlar demand gt 1-17/src/pages/TrainerBookings.tsx,
    Cenlar demand gt 1-17/src/pages/ClientPassport.tsx
  </files>
  <read_first>
    Cenlar demand gt 1-17/src/pages/TrainerBookings.tsx,
    Cenlar demand gt 1-17/src/pages/ClientPassport.tsx,
    Cenlar demand gt 1-17/src/components/shared/ClientWorkoutSummary.tsx
  </read_first>
  <action>
    **TrainerBookings.tsx:**
    1. Add import: `import ClientWorkoutSummary from '@/components/shared/ClientWorkoutSummary';`
    2. Find the booking detail expansion area where SessionLogPanel is rendered (around line 556). It currently renders inside a condition `booking.status === 'completed'`.
    3. Add ClientWorkoutSummary ABOVE the SessionLogPanel, outside the completed-status condition, so trainers see client workout history for ANY booking status (upcoming or completed). Place it inside the expanded booking detail section.
    4. Render: `<ClientWorkoutSummary clientId={booking.client_id} title="Client's Recent Workouts" />`
    5. Add a small margin-bottom (mb-4) between ClientWorkoutSummary and the existing SessionLogPanel section.

    This placement means: when a trainer expands a booking detail, they first see the client's recent workout history (read-only, from the client's own logs), then below that see the trainer's own session notes (editable, from session_logs).

    **ClientPassport.tsx:**
    1. Add import: `import ClientWorkoutSummary from '@/components/shared/ClientWorkoutSummary';`
    2. Find the end of the existing fitness profile content (after the save/edit sections, before the closing container div).
    3. Add a new section with a top border separator (border-t pt-6 mt-6): `<ClientWorkoutSummary clientId={user!.id} title="My Workout History" />`
    4. This shows the client's own workout logs at the bottom of their Fitness Passport, giving trainers viewing the passport a complete picture.

    Do NOT modify any existing functionality in either file. Only add the import and the component render.
  </action>
  <verify>
    <automated>cd "Cenlar demand gt 1-17" && grep "ClientWorkoutSummary" src/pages/TrainerBookings.tsx && grep "ClientWorkoutSummary" src/pages/ClientPassport.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "import ClientWorkoutSummary" src/pages/TrainerBookings.tsx confirms import
    - grep "ClientWorkoutSummary" src/pages/TrainerBookings.tsx shows component render with clientId prop
    - grep "client_id" src/pages/TrainerBookings.tsx near ClientWorkoutSummary confirms correct prop source
    - grep "import ClientWorkoutSummary" src/pages/ClientPassport.tsx confirms import
    - grep "ClientWorkoutSummary" src/pages/ClientPassport.tsx shows component render
    - grep "user" src/pages/ClientPassport.tsx near ClientWorkoutSummary confirms correct userId source
    - Existing SessionLogPanel in TrainerBookings remains unchanged
    - Existing fitness profile sections in ClientPassport remain unchanged
  </acceptance_criteria>
  <done>
    TrainerBookings shows client's recent workout history in expanded booking details (above session notes). ClientPassport shows workout history at the bottom of the Fitness Passport. Trainers get full visibility into client workout continuity across sessions.
  </done>
</task>

</tasks>

<verification>
- `cd "Cenlar demand gt 1-17" && npx vitest run` full suite passes (no regressions)
- ClientWorkoutSummary fetches last 5 logs with exercises and renders expandable cards
- TrainerBookings renders ClientWorkoutSummary in booking detail expansion
- ClientPassport renders ClientWorkoutSummary below fitness profile
- ExerciseDiagram and formatSet used in the summary display
- Trainer RLS allows reading client workout logs (verified by migration in Plan 01)
</verification>

<success_criteria>
- Trainer sees client's last 5 workout sessions when expanding a booking detail
- Client Fitness Passport page shows workout history section at the bottom
- Each session is expandable with exercise illustrations and set details
- Empty state handled gracefully when client has no logs
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/38-client-workout-log-exercise-diagrams/38-03-SUMMARY.md`
</output>
