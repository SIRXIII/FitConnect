---
phase: 25
slug: ai-trainer-client-matching
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-19
---

# Phase 25 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (Tailwind v4 CSS-first, no shadcn) |
| Preset | not applicable |
| Component library | none (custom components only) |
| Icon library | lucide-react (v0.555.0) |
| Font | Inter (sans), Cormorant Garamond (serif) — already loaded via Google Fonts in index.css |

Source: `src/index.css` design tokens + `package.json`.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, bullet separator gaps |
| sm | 8px | Explanation bullet padding, avatar-to-text gap |
| md | 16px | Card internal padding (horizontal), between explanation lines |
| lg | 24px | Card internal padding (vertical), section header margin-bottom |
| xl | 32px | Horizontal gap between carousel cards (desktop) |
| 2xl | 48px | Carousel section top padding |
| 3xl | 64px | Not used in this phase |

Exceptions: Horizontal scroll snap container uses `scroll-padding-left: 16px` on mobile (aligns first card to viewport edge at 16px inset).

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 (regular) | 1.5 |
| Label | 10px | 400 (regular) | 1.0 — uppercase tracking-widest |
| Heading | 20px | 500 (medium) | 1.2 — serif Cormorant Garamond italic |
| Display | 28px | 500 (medium) | 1.2 — serif, section title "Recommended for You" |

Notes:
- Match score percentage: 28px serif italic `text-accent` — visually prominent, consistent with rate display pattern in BestDeals/TrainerCard
- Match tier label ("Great Match", "Good Match", "Fair Match"): 10px uppercase tracking-widest, `text-ink/40` below score
- Explanation bullets: 14px regular (400), `text-ink/60`, prefixed with a `·` separator dot — merged from prior 12px spec
- Trainer name on match card: 14px `font-medium text-ink` (Inter sans, not serif) — merged from prior 16px spec; weight 500 carries hierarchy
- Specialty line: 10px uppercase `tracking-widest text-ink/40` — same pattern as existing cards

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#FDFCFB` (`--color-paper`) | Page background, card background |
| Secondary (30%) | `#1A1A1A` (`--color-ink`) | Text, borders (`border-ink/10`) |
| Accent (10%) | `#C5A059` (`--color-accent`) | Match score percentage, score progress arc, trainer name hover, rate display, "Complete Passport" CTA border-bottom underline |
| Destructive | not applicable in this phase | — |

Accent reserved for: match score percentage value, score ring/arc stroke (if progress ring variant used), trainer name hover state on match card, hourly rate display on match card, and the "Complete your Fitness Passport" prompt CTA text underline. Not applied to explanation bullets, section labels, or card borders.

Source: `src/index.css` `@theme` block, consistent with existing TrainerCard and BestDeals patterns.

---

## Component Inventory

### RecommendedCarousel (new)

**Placement:** Above the trainer grid in `SearchSection.tsx`, below the filter bar, above the list/map toggle.

**States:**
1. **Loading** — 3 × `TrainerCardSkeleton` (reuse `src/components/skeleton/TrainerCardSkeleton.tsx`) in a horizontal flex row, no scroll
2. **Carousel populated** — horizontal scroll container, `overflow-x-auto`, `scroll-snap-type x mandatory`, 3 `MatchCard` items
3. **Passport gate** — single `PassportPromptCard` replacing the carousel (inline, not a modal)
4. **Fewer than 3 matches** — show available matches (minimum 1); if 0 matches exist above threshold, hide section entirely (return null)
5. **Cache hit** — no visual distinction; same carousel UI, data sourced from cache

**Layout:** On desktop (md+): 3-column horizontal flex with `gap-8`. On mobile: horizontal scroll with snap, each card `w-72` (288px), `flex-shrink-0`.

**Section header (when carousel visible):**
```
[Sparkles icon 14px, text-accent]  MATCHED FOR YOU  [10px uppercase tracking-[0.3em] text-ink/40]
"Recommended for You"  [28px serif italic]
```
No "See all" link — top 3 is intentional curation, not a filtered view.

---

### MatchCard (new)

Dimensions: fixed `w-full` on desktop grid cell, `w-72` on mobile scroll. Internal: `border border-ink/10 p-6 space-y-5 hover:border-ink/25 transition-colors duration-500`.

**Anatomy (top to bottom):**

1. **Avatar + name row** — 48px avatar (rounded-full), trainer name 14px medium (500), specialty 10px uppercase. Same structure as DealCard in BestDeals.tsx.
2. **Rate row** — `$X/hr` in 20px serif italic `text-accent`, consistent with TrainerCard rate display.
3. **Match score row** — score percentage in 28px serif italic `text-accent`, tier label in 10px uppercase `text-ink/40` to the right or below. Example: `87%  ·  Great Match`.
   - Tier thresholds (from CONTEXT.md): Great Match 80%+, Good Match 60–79%, Fair Match 40–59%.
   - No color change per tier — accent is always accent; tier label text communicates tier.
4. **Explanation bullets** — 2–3 lines, 14px regular (400) `text-ink/60`, each line prefixed with `·` dot. Line examples: `· Matches your HIIT goals`, `· Within your $50–80 range`.
5. **Divider** — `border-t border-ink/5 pt-4`
6. **View Profile CTA** — full-width ghost button: `border border-ink/10 py-4 text-[10px] uppercase tracking-[0.3em] hover:bg-ink hover:text-white transition-all duration-500`. Same style as TrainerCard's "View Profile" button.

---

### PassportPromptCard (new)

Shown when client has fewer than 3 key fields filled (fitness level + at least 1 goal + preferred workout type).

**Structure:** Single card, same border and padding as MatchCard. No avatar, no score. Contains:
- Icon: `Sparkles` or `User` (16px, `text-accent`)
- Heading (14px `font-medium text-ink`): "Your matches are waiting."
- Body (14px `text-ink/60`): "Complete your Fitness Passport so we can recommend trainers matched to your goals, intensity preference, and budget."
- CTA link (styled as text underline in accent): "Complete Fitness Passport →" — navigates to `/client/passport`

**No modal, no overlay** — inline card in the section slot where the carousel would be.

---

## Interaction Contracts

### Carousel scroll (mobile)
- `scroll-snap-type: x mandatory` on container
- `scroll-snap-align: start` on each card
- First card aligns to left edge with 16px inset (`scroll-padding-left: 1rem`)
- No custom scrollbar (use native, hidden via `scrollbar-hide` utility or equivalent)
- No pagination dots — 3 items is always the max, native scroll sufficient

### Hover states
- Card border: `border-ink/10` → `border-ink/25` on hover (transition-colors duration-500) — same as DealCard
- Trainer name: `text-ink` → `text-accent` on hover (transition-colors) — same as TrainerCard
- View Profile button: `bg-transparent border-ink/10` → `bg-ink text-white` on hover (transition-all duration-500)

### Loading state
- Show 3 × `TrainerCardSkeleton` immediately on mount
- Replace with carousel or prompt card once data resolves
- No error banner for match fetch failure — section returns null silently (not a critical path; main trainer grid remains visible)

### Cache behavior (no visual contract required)
- 24-hour cache is server-side; client sees same carousel UI regardless of cache hit/miss
- No "last updated" timestamp shown to user

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Section label (above heading) | "Matched for You" |
| Section heading | "Recommended for You" |
| Passport prompt heading | "Your matches are waiting." |
| Passport prompt body | "Complete your Fitness Passport so we can recommend trainers matched to your goals, intensity preference, and budget." |
| Passport prompt CTA | "Complete Fitness Passport" |
| Card CTA | "View Profile" |
| Match tier — 80%+ | "Great Match" |
| Match tier — 60–79% | "Good Match" |
| Match tier — 40–59% | "Fair Match" |
| Empty state (0 trainers match above threshold) | Section hidden — return null, no message shown |
| Error state (fetch failure) | Section hidden — return null, main grid unaffected |
| Explanation bullet — price | "Within your $X–Y range" |
| Explanation bullet — goals | "Matches your [Goal Name] goals" |
| Explanation bullet — workout type | "Offers [Workout Type] sessions" |

Notes:
- Explanation bullets use "your" (possessive, personalised) — consistent with CONTEXT.md specifics ("Matches YOUR HIIT goals")
- "Complete Fitness Passport" not "Fill in your profile" — matches existing in-app label from Phase 23.1

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| third-party | none | not applicable |

No third-party component registries used in this phase. All new components are built from project primitives (Tailwind v4 utilities, Lucide icons, Framer Motion).

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
