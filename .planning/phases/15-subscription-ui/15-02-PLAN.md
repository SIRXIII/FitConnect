---
phase: 15-subscription-ui
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - Cenlar demand gt 1-17/src/pages/Pricing.tsx
  - Cenlar demand gt 1-17/src/components/subscription/PricingTable.tsx
  - Cenlar demand gt 1-17/src/components/subscription/PlanCard.tsx
  - Cenlar demand gt 1-17/src/components/subscription/BillingToggle.tsx
  - Cenlar demand gt 1-17/src/App.tsx
  - Cenlar demand gt 1-17/src/components/shared/LockedFeatureBanner.tsx
autonomous: true
requirements: [BILL-01, BILL-04]

must_haves:
  truths:
    - "Visiting /pricing shows a tier comparison with Free, Pro (highlighted), and Elite plans"
    - "Toggling to Annual shows 'Save 20%' badge and per-month equivalent prices"
    - "Clicking 'Start Free Trial' on Pro calls startTrial('pro', interval) and shows success toast"
    - "After trial start, dashboard shows tier badge 'Pro -- Trialing' with trial end date"
    - "Unauthenticated visitors see pricing page; CTA redirects to /login?redirect=/pricing"
    - "Already-subscribed trainers see 'Current Plan' badge instead of 'Start Trial'"
    - "LockedFeatureBanner links to /pricing for upgrade CTA"
  artifacts:
    - path: "Cenlar demand gt 1-17/src/pages/Pricing.tsx"
      provides: "Public pricing page with billing toggle and trial start flow"
      min_lines: 80
    - path: "Cenlar demand gt 1-17/src/components/subscription/PricingTable.tsx"
      provides: "3-column tier comparison grid"
      min_lines: 30
    - path: "Cenlar demand gt 1-17/src/components/subscription/PlanCard.tsx"
      provides: "Individual plan card with features list and CTA"
      min_lines: 40
    - path: "Cenlar demand gt 1-17/src/components/subscription/BillingToggle.tsx"
      provides: "Monthly/Annual switch with Save 20% badge"
      min_lines: 20
  key_links:
    - from: "Cenlar demand gt 1-17/src/pages/Pricing.tsx"
      to: "src/lib/subscription.ts"
      via: "import { PRICING_DATA, startTrial, BillingInterval }"
      pattern: "import.*PRICING_DATA.*subscription"
    - from: "Cenlar demand gt 1-17/src/pages/Pricing.tsx"
      to: "src/stores/auth.ts"
      via: "useAuthStore for user/trainerProfile state"
      pattern: "useAuthStore"
    - from: "Cenlar demand gt 1-17/src/App.tsx"
      to: "src/pages/Pricing.tsx"
      via: "Route path='/pricing' (public, no ProtectedRoute)"
      pattern: "path=.*pricing"
    - from: "Cenlar demand gt 1-17/src/components/shared/LockedFeatureBanner.tsx"
      to: "/pricing"
      via: "Link to pricing page for upgrade CTA"
      pattern: "pricing"
---

<objective>
Build the public pricing page with tier comparison, billing toggle, and trial start flow.

Purpose: Trainers discover subscription options and start a free trial directly from the pricing page (success criteria 1 and 2). LockedFeatureBanner gets its upgrade CTA link.
Output: Pricing.tsx page, PricingTable, PlanCard, BillingToggle components, /pricing route in App.tsx, upgrade link in LockedFeatureBanner.
</objective>

<execution_context>
@/Users/xfiles/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xfiles/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-subscription-ui/15-RESEARCH.md
@.planning/phases/15-subscription-ui/15-01-SUMMARY.md

@Cenlar demand gt 1-17/src/App.tsx
@Cenlar demand gt 1-17/src/pages/TrainerDashboard.tsx
@Cenlar demand gt 1-17/src/stores/auth.ts
@Cenlar demand gt 1-17/src/lib/tierGates.ts
@Cenlar demand gt 1-17/src/components/shared/LockedFeatureBanner.tsx

<interfaces>
<!-- From plan 15-01 outputs -->

From src/lib/subscription.ts (created in 15-01):
```typescript
export type BillingInterval = 'month' | 'year';
export interface PlanPricing {
  tier: 'free' | 'pro' | 'elite';
  name: string;
  monthlyPrice: number;
  annualPrice: number;
  annualMonthly: number;
  features: string[];
  highlighted?: boolean;
}
export const PRICING_DATA: PlanPricing[];
export async function startTrial(tier: 'pro' | 'elite', interval: BillingInterval): Promise<{ subscriptionId: string; status: string }>;
export async function getPortalUrl(): Promise<{ url: string }>;
export function featuresLostOnDowngrade(from: Tier, to: Tier): TierFeature[];
```

From src/stores/auth.ts:
```typescript
export const useAuthStore = create<AuthState>(...);
// AuthState.user: User | null
// AuthState.trainerProfile: TrainerProfile | null (has subscription_tier, subscription_status, trial_ends_at)
// AuthState.fetchProfile(userId): Promise<void>
// AuthState.profile: Profile | null (has role)
```

From src/lib/tierGates.ts:
```typescript
export type Tier = 'free' | 'pro' | 'elite';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BillingToggle, PlanCard, and PricingTable components</name>
  <files>
    Cenlar demand gt 1-17/src/components/subscription/BillingToggle.tsx
    Cenlar demand gt 1-17/src/components/subscription/PlanCard.tsx
    Cenlar demand gt 1-17/src/components/subscription/PricingTable.tsx
  </files>
  <action>
Create `src/components/subscription/` directory and three components:

**BillingToggle.tsx** -- controlled component:
- Props: `{ interval: BillingInterval; onToggle: (interval: BillingInterval) => void }`
- Two side-by-side buttons: "Monthly" and "Annual"
- Active button: `bg-ink text-white`, inactive: `text-ink/40 hover:text-ink`
- When Annual is active, show a small badge: "Save 20%" in `text-accent` to the right of the toggle
- Use `text-[10px] uppercase tracking-[0.2em] font-medium` for button labels (matching tab bar style)
- Container: `flex items-center gap-4`

**PlanCard.tsx** -- individual tier card:
- Props: `{ plan: PlanPricing; interval: BillingInterval; currentTier: Tier | null; isTrialing: boolean; onStartTrial: (tier: 'pro' | 'elite', interval: BillingInterval) => void; loading: boolean; isAuthenticated: boolean }`
- Card: `border border-ink/10 p-8 space-y-6` with `border-accent/30 ring-1 ring-accent/10` when `plan.highlighted`
- Display plan name as `text-xl serif font-light italic`
- Display price: if interval=month show `$X/mo`, if interval=year show `$X.XX/mo` (annualMonthly) with small text `billed $X.XX/year` below
- Free tier shows `$0` with "Free forever" subtext
- Features list: checkmarks (Lucide `Check` icon) with `text-sm text-ink/60`
- CTA button logic:
  - If `plan.tier === 'free'`: no CTA button (or "Current Plan" label if currentTier is free)
  - If `currentTier === plan.tier` (active subscriber): show "Current Plan" label (no button), styled as `text-[10px] uppercase tracking-[0.2em] text-ink/40`
  - If `!isAuthenticated` and tier is pro/elite: "Start Free Trial" button that links to `/login?redirect=/pricing`
  - If authenticated, currentTier is free/null, and tier is pro/elite: "Start Free Trial" button that calls `onStartTrial(plan.tier, interval)`, disabled when `loading`
  - If authenticated and already subscribed to different paid tier: "Manage Subscription" label (handled in dashboard)
- CTA button style: `bg-ink text-white px-10 py-3 text-[11px] uppercase tracking-[0.2em] font-medium hover:bg-accent transition-colors w-full`
- Disabled state: `opacity-50 cursor-not-allowed`

**PricingTable.tsx** -- grid layout:
- Props: `{ interval: BillingInterval; currentTier: Tier | null; isTrialing: boolean; onStartTrial: (tier: 'pro' | 'elite', interval: BillingInterval) => void; loading: boolean; isAuthenticated: boolean }`
- Imports PRICING_DATA from `@/lib/subscription`
- Renders `grid grid-cols-1 md:grid-cols-3 gap-6` with a PlanCard for each item in PRICING_DATA
- Passes all props through to each PlanCard
  </action>
  <verify>
    <automated>cd "Cenlar demand gt 1-17" && ls src/components/subscription/BillingToggle.tsx src/components/subscription/PlanCard.tsx src/components/subscription/PricingTable.tsx && grep -c "export" src/components/subscription/PlanCard.tsx</automated>
  </verify>
  <done>Three subscription components exist with correct props interfaces, editorial design system styling, and CTA button logic handling all states (unauthenticated, free, subscribed, trialing)</done>
</task>

<task type="auto">
  <name>Task 2: Create Pricing page, add /pricing route, and wire LockedFeatureBanner upgrade CTA</name>
  <files>
    Cenlar demand gt 1-17/src/pages/Pricing.tsx
    Cenlar demand gt 1-17/src/App.tsx
    Cenlar demand gt 1-17/src/components/shared/LockedFeatureBanner.tsx
  </files>
  <action>
**Pricing.tsx** -- public pricing page:
- Import `useAuthStore` for user, trainerProfile, fetchProfile
- Import `BillingToggle`, `PricingTable` from `@/components/subscription/`
- Import `startTrial`, `BillingInterval` from `@/lib/subscription`
- Import `toast` from `sonner`
- Import `useNavigate` from `react-router-dom`
- Import `Tier` from `@/lib/tierGates`
- State: `interval: BillingInterval` (default 'month' per research recommendation -- lower sticker shock), `loading: boolean`
- Derive `currentTier`: if trainerProfile exists, use `trainerProfile.subscription_tier as Tier`, else null
- Derive `isTrialing`: `trainerProfile?.subscription_status === 'trialing'`
- Derive `isAuthenticated`: `!!user`
- `handleStartTrial` async function:
  1. Set loading true
  2. try: await startTrial(tier, interval)
  3. toast.success('Trial started! Setting up your account...')
  4. await new Promise(r => setTimeout(r, 2000)) -- give webhook time
  5. await fetchProfile(user!.id)
  6. navigate('/trainer/dashboard?tab=subscription')
  7. catch: toast.error(err.message or 'Failed to start trial')
  8. finally: set loading false
- Layout: `min-h-screen bg-paper pt-32 pb-20 px-6`
- Max width container: `max-w-5xl mx-auto space-y-12`
- Header: `text-3xl serif font-light italic text-ink text-center` "Choose Your Plan"
- Subtitle: `text-sm text-ink/50 text-center` "Start with a 30-day free trial. No credit card required."
- BillingToggle centered: `flex justify-center`
- PricingTable below toggle
- If isTrialing, show a small note below the table: `text-xs text-ink/40 text-center` "You're currently trialing {tierName}. Manage your subscription from the dashboard."

**App.tsx** modifications:
- Import Pricing from `@/pages/Pricing`
- Add route in the Public section (after the `/auth/callback` route, before Onboarding):
  `<Route path="/pricing" element={<Pricing />} />`
- This is a PUBLIC route -- no ProtectedRoute wrapper (per research recommendation)

**LockedFeatureBanner.tsx** modification:
- Find the comment "Upgrade CTA placeholder -- Phase 15 will add the pricing page link"
- Replace with a Link to `/pricing`:
  ```tsx
  <Link to="/pricing" className="text-accent underline underline-offset-2 text-xs">
    View upgrade options
  </Link>
  ```
- Add `import { Link } from 'react-router-dom'` at top
  </action>
  <verify>
    <automated>cd "Cenlar demand gt 1-17" && grep "pricing" src/App.tsx && grep "pricing" src/components/shared/LockedFeatureBanner.tsx && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>/pricing route serves the Pricing page publicly; BillingToggle defaults to monthly; "Start Free Trial" calls startTrial with tier+interval then navigates to dashboard; LockedFeatureBanner links to /pricing</done>
</task>

</tasks>

<verification>
- `curl -s http://localhost:5173/pricing` renders (dev server) -- or check route exists in App.tsx
- BillingToggle defaults to monthly; switching to annual shows "Save 20%" and annual equivalent prices
- PlanCard shows "Current Plan" when trainer's tier matches, "Start Free Trial" when free
- LockedFeatureBanner has a link to /pricing
- `npx vitest run` passes (no regressions)
</verification>

<success_criteria>
- /pricing is a public route showing 3 tier cards with feature lists
- Monthly/Annual toggle works with "Save 20%" badge on annual
- Pro card is visually highlighted (accent border)
- "Start Free Trial" calls startTrial, shows toast, waits 2s, refreshes profile, navigates to dashboard
- Unauthenticated users clicking "Start Free Trial" are sent to /login?redirect=/pricing
- LockedFeatureBanner now has a working "View upgrade options" link to /pricing
</success_criteria>

<output>
After completion, create `.planning/phases/15-subscription-ui/15-02-SUMMARY.md`
</output>
