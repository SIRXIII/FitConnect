---
phase: 27-location-based-notifications
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - "Cenlar demand gt 1-17/src/components/client/NotificationPreferencesSection.tsx"
  - "Cenlar demand gt 1-17/src/components/search/LookingNowToggle.tsx"
  - "Cenlar demand gt 1-17/src/pages/ClientDashboard.tsx"
  - "Cenlar demand gt 1-17/src/components/search/SearchSection.tsx"
  - "Cenlar demand gt 1-17/src/components/layout/Navbar.tsx"
autonomous: false
requirements: [NOTIF-01, NOTIF-02, NOTIF-03, NOTIF-04, NOTIF-06]

must_haves:
  truths:
    - "Client can see an 'Alerts' tab on the Client Dashboard with notification preferences form"
    - "Client can enter a neighborhood via Places Autocomplete and set a radius slider (1-10 miles)"
    - "Client can toggle location-based notifications on/off via a master switch"
    - "Client sees a 'Looking Now' toggle in the search/map area with pulsing indicator"
    - "Notification for type trainer_live_nearby renders with location icon in Navbar dropdown"
    - "Preferences must be configured before alerts fire (UI gate: save button disabled when no area set)"
  artifacts:
    - path: "Cenlar demand gt 1-17/src/components/client/NotificationPreferencesSection.tsx"
      provides: "Notification preferences form with Places Autocomplete, radius slider, master toggle"
      exports: ["NotificationPreferencesSection"]
    - path: "Cenlar demand gt 1-17/src/components/search/LookingNowToggle.tsx"
      provides: "GPS mode toggle with pulsing animation"
      exports: ["LookingNowToggle"]
    - path: "Cenlar demand gt 1-17/src/pages/ClientDashboard.tsx"
      provides: "Alerts tab added to tab navigation"
      contains: "Alerts"
    - path: "Cenlar demand gt 1-17/src/components/search/SearchSection.tsx"
      provides: "LookingNowToggle integrated into search area"
      contains: "LookingNowToggle"
    - path: "Cenlar demand gt 1-17/src/components/layout/Navbar.tsx"
      provides: "trainer_live_nearby notification styling with MapPin icon"
      contains: "trainer_live_nearby"
  key_links:
    - from: "NotificationPreferencesSection"
      to: "useNotificationPreferences hook"
      via: "import and call savePreferences/toggleEnabled"
      pattern: "useNotificationPreferences"
    - from: "LookingNowToggle"
      to: "useLookingNow hook"
      via: "import and call activate/deactivate"
      pattern: "useLookingNow"
    - from: "ClientDashboard"
      to: "NotificationPreferencesSection"
      via: "Alerts tab renders the component"
      pattern: "NotificationPreferencesSection"
    - from: "SearchSection"
      to: "LookingNowToggle"
      via: "Rendered in filter/map area alongside MapListToggle"
      pattern: "LookingNowToggle"
---

<objective>
Build the UI layer for location-based notifications: a preferences form on the Client Dashboard, a "Looking Now" GPS toggle in the search area, and enhanced notification rendering for the new notification type.

Purpose: Gives clients full control over location alerts — area selection, radius tuning, master toggle, and live GPS mode — and makes trainer-live-nearby notifications visually distinct in the notification dropdown.
Output: Three new components, two modified pages, one modified component.
</objective>

<execution_context>
@/Users/xfiles/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xfiles/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-location-based-notifications/27-CONTEXT.md
@.planning/phases/27-location-based-notifications/27-RESEARCH.md
@.planning/phases/27-location-based-notifications/27-01-SUMMARY.md

<interfaces>
<!-- From Plan 27-01 -->
From Cenlar demand gt 1-17/src/hooks/useNotificationPreferences.ts:
```typescript
export function useNotificationPreferences(): {
  preferences: ClientNotificationPreferences | null;
  loading: boolean;
  savePreferences: (data: Partial<ClientNotificationPreferences>) => Promise<void>;
  toggleEnabled: (enabled: boolean) => Promise<void>;
  isConfigured: boolean;
  refetch: () => Promise<void>;
};
```

From Cenlar demand gt 1-17/src/hooks/useLookingNow.ts:
```typescript
export function useLookingNow(): {
  isActive: boolean;
  livePosition: { lat: number; lng: number } | null;
  activate: () => void;
  deactivate: () => void;
};
```

From Cenlar demand gt 1-17/src/types/notifications.ts:
```typescript
export interface ClientNotificationPreferences {
  id: string;
  user_id: string;
  notif_enabled: boolean;
  area_label: string | null;
  area_lat: number | null;
  area_lng: number | null;
  notif_radius_miles: number;
  created_at: string;
  updated_at: string;
}
```

<!-- Existing components being modified -->
From Cenlar demand gt 1-17/src/pages/ClientDashboard.tsx:
```typescript
// Tabs: 'overview' | 'progress' — add 'alerts'
const [activeTab, setActiveTab] = useState<'overview' | 'progress'>('overview');
```

From Cenlar demand gt 1-17/src/components/search/SearchSection.tsx:
```typescript
// Has MapListToggle, MapView, filters area
// viewMode state: 'list' | 'map'
// Imports: MapListToggle, MapView, RecommendedCarousel
```

From Cenlar demand gt 1-17/src/components/layout/Navbar.tsx:
```typescript
// Notification dropdown renders notif.title, notif.message, notif.link
// notif.type available but not currently used for styling differentiation
```

From Cenlar demand gt 1-17/src/components/trainer/WorkoutLocationsManager.tsx:
```typescript
// Places Autocomplete pattern:
// const placesLib = useMapsLibrary('places');
// Wrapped in <APIProvider apiKey={import.meta.env.VITE_GOOGLE_MAPS_API_KEY} libraries={['places']}>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NotificationPreferencesSection + LookingNowToggle components</name>
  <files>
    Cenlar demand gt 1-17/src/components/client/NotificationPreferencesSection.tsx,
    Cenlar demand gt 1-17/src/components/search/LookingNowToggle.tsx
  </files>
  <read_first>
    Cenlar demand gt 1-17/src/components/trainer/WorkoutLocationsManager.tsx,
    Cenlar demand gt 1-17/src/hooks/useNotificationPreferences.ts,
    Cenlar demand gt 1-17/src/hooks/useLookingNow.ts,
    Cenlar demand gt 1-17/src/pages/ClientDashboard.tsx
  </read_first>
  <acceptance_criteria>
    - grep "NotificationPreferencesSection" in the component file confirms export
    - grep "useNotificationPreferences" in NotificationPreferencesSection.tsx confirms hook usage
    - grep "APIProvider" in NotificationPreferencesSection.tsx confirms Places wrapping (Pitfall 6)
    - grep "fetchAutocompleteSuggestions" in NotificationPreferencesSection.tsx confirms autocomplete
    - grep "notif_radius_miles" in NotificationPreferencesSection.tsx confirms radius slider
    - grep "notif_enabled" in NotificationPreferencesSection.tsx confirms master toggle
    - grep "isConfigured" in NotificationPreferencesSection.tsx confirms gate check
    - grep "LookingNowToggle" in the toggle file confirms export
    - grep "useLookingNow" in LookingNowToggle.tsx confirms hook usage
    - grep "animate" in LookingNowToggle.tsx confirms pulsing indicator (framer-motion or Tailwind animate-pulse)
  </acceptance_criteria>
  <action>
    **NotificationPreferencesSection.tsx:**
    - Wrap entire component in `<APIProvider apiKey={import.meta.env.VITE_GOOGLE_MAPS_API_KEY} libraries={['places']}>` (Pitfall 6 from research -- ClientDashboard has no APIProvider ancestor)
    - Use `useNotificationPreferences()` hook for state
    - Use `useMapsLibrary('places')` for Places Autocomplete (same pattern as WorkoutLocationsManager.tsx)
    - Layout sections:
      1. **Master toggle:** Labeled "Location-Based Alerts" with ON/OFF switch. Calls `toggleEnabled()`. When OFF, rest of form is visually disabled (opacity-50 pointer-events-none).
      2. **Area selector:** Text input with Google Places Autocomplete dropdown. On selection, resolve coordinates via `PlacesService.getDetails()` and store `area_label`, `area_lat`, `area_lng`. Show selected area as a pill/chip below input.
      3. **Radius slider:** `<input type="range" min={1} max={10} step={1}>` with label showing "{value} miles". Default 5.
      4. **Save button:** Calls `savePreferences()` with all fields. Disabled when `area_lat` is null (gate for NOTIF-06 -- preferences must be set before alerts fire). Show toast.success on save.
    - Styling: Match existing ClientDashboard aesthetic -- border border-ink/10, text-xs uppercase tracking, serif headings
    - Show a helper text: "Get notified when trainers go live near your area" under the section heading

    **LookingNowToggle.tsx:**
    - Use `useLookingNow()` hook
    - Render a button/toggle: "Looking Now" text with a pulsing dot when active
    - When inactive: muted button with MapPin icon, text "Looking Now"
    - When active: accent-colored button with `animate-pulse` dot, text "Looking Now" in accent color
    - onClick: toggle between activate() and deactivate()
    - Show `livePosition` coordinates subtly when active (e.g., "Using your location" helper text)
    - If geolocation is not supported: render nothing (graceful degradation)
    - Use Tailwind for the pulsing animation: `animate-pulse` on a small dot element
  </action>
  <verify>
    <automated>grep -c "NotificationPreferencesSection" "Cenlar demand gt 1-17/src/components/client/NotificationPreferencesSection.tsx" && grep -c "LookingNowToggle" "Cenlar demand gt 1-17/src/components/search/LookingNowToggle.tsx" && grep -c "APIProvider" "Cenlar demand gt 1-17/src/components/client/NotificationPreferencesSection.tsx" && grep -c "fetchAutocompleteSuggestions" "Cenlar demand gt 1-17/src/components/client/NotificationPreferencesSection.tsx"</automated>
  </verify>
  <done>NotificationPreferencesSection renders complete form with Places Autocomplete area selector, radius slider, master toggle, and save gate. LookingNowToggle renders GPS toggle with pulsing indicator.</done>
</task>

<task type="auto">
  <name>Task 2: Wire into ClientDashboard, SearchSection, and Navbar</name>
  <files>
    Cenlar demand gt 1-17/src/pages/ClientDashboard.tsx,
    Cenlar demand gt 1-17/src/components/search/SearchSection.tsx,
    Cenlar demand gt 1-17/src/components/layout/Navbar.tsx
  </files>
  <read_first>
    Cenlar demand gt 1-17/src/pages/ClientDashboard.tsx,
    Cenlar demand gt 1-17/src/components/search/SearchSection.tsx,
    Cenlar demand gt 1-17/src/components/layout/Navbar.tsx,
    Cenlar demand gt 1-17/src/components/client/NotificationPreferencesSection.tsx,
    Cenlar demand gt 1-17/src/components/search/LookingNowToggle.tsx
  </read_first>
  <acceptance_criteria>
    - grep "alerts" in ClientDashboard.tsx confirms new tab value in the type union
    - grep "NotificationPreferencesSection" in ClientDashboard.tsx confirms import and render
    - grep "LookingNowToggle" in SearchSection.tsx confirms import and render
    - grep "trainer_live_nearby" in Navbar.tsx confirms type-specific notification styling
    - grep "MapPin" in Navbar.tsx confirms location icon for the new notification type
  </acceptance_criteria>
  <action>
    **ClientDashboard.tsx:**
    - Widen activeTab type: `'overview' | 'progress' | 'alerts'`
    - Add "Alerts" tab button in the tab navigation bar (same styling as Overview/Progress buttons): Bell icon from lucide-react + "Alerts" text
    - In the tab content area, add: `activeTab === 'alerts' && <NotificationPreferencesSection />`
    - Import `NotificationPreferencesSection` from `@/components/client/NotificationPreferencesSection`
    - Import `Bell` from `lucide-react`

    **SearchSection.tsx:**
    - Import `LookingNowToggle` from `./LookingNowToggle`
    - Render `<LookingNowToggle />` next to the existing `<MapListToggle>` component in the filter/controls area (they should sit side by side)
    - Only render for client role (check `useAuthStore().profile?.role === 'client'`). Trainers do not need "Looking Now".

    **Navbar.tsx:**
    - Import `MapPin` from `lucide-react`
    - In the notification rendering map, add a conditional icon before the title:
      - If `notif.type === 'trainer_live_nearby'`: show `<MapPin size={12} className="text-accent shrink-0" />` in the flex row alongside the title
      - For all other types: keep existing behavior (no icon change)
    - This makes location notifications visually distinct with a map pin icon
  </action>
  <verify>
    <automated>grep -c "alerts" "Cenlar demand gt 1-17/src/pages/ClientDashboard.tsx" && grep -c "NotificationPreferencesSection" "Cenlar demand gt 1-17/src/pages/ClientDashboard.tsx" && grep -c "LookingNowToggle" "Cenlar demand gt 1-17/src/components/search/SearchSection.tsx" && grep -c "trainer_live_nearby" "Cenlar demand gt 1-17/src/components/layout/Navbar.tsx"</automated>
  </verify>
  <done>ClientDashboard has Alerts tab rendering NotificationPreferencesSection. SearchSection shows LookingNowToggle for clients. Navbar renders MapPin icon for trainer_live_nearby notifications.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify notification preferences UI and Looking Now toggle</name>
  <files>
    Cenlar demand gt 1-17/src/pages/ClientDashboard.tsx,
    Cenlar demand gt 1-17/src/components/search/SearchSection.tsx,
    Cenlar demand gt 1-17/src/components/layout/Navbar.tsx
  </files>
  <action>
    Human visual verification of the complete location-based notifications UI flow. No code changes -- purely testing that Tasks 1 and 2 produced a working user experience.
  </action>
  <what-built>
    Complete location-based notifications UI: preferences form on Client Dashboard "Alerts" tab, "Looking Now" GPS toggle on search page, and enhanced notification rendering in Navbar dropdown.
  </what-built>
  <how-to-verify>
    1. Start dev server: `cd "Cenlar demand gt 1-17" && npm run dev`
    2. Log in as a client user
    3. Navigate to Client Dashboard -- verify "Alerts" tab appears alongside Overview and Progress
    4. Click Alerts tab -- verify:
       - Master toggle "Location-Based Alerts" is visible
       - Area text input with autocomplete is present
       - Radius slider (1-10 miles) works
       - Save button is disabled when no area is set (gate check)
       - After entering an area and enabling toggle, Save button becomes active
    5. Navigate to /trainers search page -- verify:
       - "Looking Now" toggle appears near the Map/List toggle
       - Clicking it requests geolocation permission
       - When active, shows pulsing indicator
       - Clicking again deactivates it
    6. (If testing with a second account) Have a trainer go live with an active location near the client's saved area -- verify notification appears in the bell dropdown with a map pin icon
  </how-to-verify>
  <verify>
    <automated>cd "Cenlar demand gt 1-17" && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>User has visually confirmed Alerts tab, preferences form, Looking Now toggle, and notification rendering all work correctly</done>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- Client Dashboard shows Alerts tab with full preferences form
- Places Autocomplete works for area selection
- Radius slider moves between 1-10 miles
- Master toggle enables/disables alerts
- Save is gated on area being set (NOTIF-06)
- LookingNowToggle appears on search page for clients only
- Navbar shows MapPin icon for trainer_live_nearby notifications
- Full test suite still passes
</verification>

<success_criteria>
- Client can configure notification preferences (area, radius, toggle) from the Alerts tab
- Client can activate "Looking Now" GPS mode from the search page
- Notification preferences must be set before alerts fire (gate enforced)
- trainer_live_nearby notifications render with a location icon in the dropdown
- No regressions in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/27-location-based-notifications/27-02-SUMMARY.md`
</output>
